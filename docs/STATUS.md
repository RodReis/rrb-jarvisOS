# STATUS.md — Kanban / Roadmap

> **Infra de CI (2026-09-06):** otimização autorizada pelo PI após a PR #308, entregue em duas PRs. A [#311](https://github.com/RodReis/rrb-jarvisOS/pull/311) eliminou a segunda execução da suíte (as guardas passaram a rodar numa chamada só) e pôs lint/typecheck e prova visual em jobs paralelos; a [#312](https://github.com/RodReis/rrb-jarvisOS/pull/312) paralelizou também as três categorias (`test-regras`, `test-banco`, `test-tela`) e reduziu o `test` a agregador, que não reexecuta a suíte. Gate único e fechado em falhas. Node 24 LTS e Supabase CLI fixada em `v2.116.0`. Medição final da #312 (run `34054899376`): workflow completo em aproximadamente 6min16s, contra 16min33s da #308; `test-banco` foi o caminho crítico, com 5min51s. Diagnóstico e critérios em [CI-PR.md](CI-PR.md). Rastreabilidade na issue [#310](https://github.com/RodReis/rrb-jarvisOS/issues/310).
> **MVP-027 definido (2026-09-06):** [SPEC-Pipeline-01 R2+E1](spec/spec-pipeline-01-politica-pr-ci.md), política genérica de PR/CI por projeto, `aprovada-pi`. A **Emenda E1 aprovada** torna o perfil e a cobertura de PR/Testing/QA/Banco pré-condições do `SLICE_ENTRY`, antes da criação da issue de Construção. Windows/PowerShell é prova obrigatória; Node legado passa à linha 24 LTS. Épico #313 e fatia M27-F01 #314 são os itens canônicos; não criar duplicatas. A emenda não altera a fila nem inicia Construção. A rotina deste repositório está no [guia do Claude Code](GUIA-PRS-CLAUDE-CODE.md).

Atualizado em: **2026-09-06**. Fonte única do índice Fatia ↔ SPEC. Estado remoto conferido nesta revisão: as **cinco fatias do MVP-025 (#238 a #242) estão `proplan:finalizado`** — aceitas pelo PI em 2026-09-03; o fechamento do épico #237 é ato dele. Nasce o **MVP-026 — Fases, modelo por fase e console da geração** (épico [#250](https://github.com/RodReis/rrb-jarvisOS/issues/250)), decidido pelo PI em 2026-09-04 após usar o app: fase no card, modelo por fase (Fable 5.1 / Sol / Opus 5 / gpt-5.5), console da geração com ferramentas, marcos Git verificados antes da Construção e Codex como provider do ponto único. **As cinco fatias do MVP-026 entregues (F01–F05); a M26-F03, a M26-F04 e a M26-F05 já estão `proplan:finalizado` — aceitas pelo PI. A **M10-F02** ([#117](https://github.com/RodReis/rrb-jarvisOS/issues/117), PR [#267](https://github.com/RodReis/rrb-jarvisOS/pull/267), `45d069c`) está entregue e em `proplan:done` — a emenda do PI de 2026-09-04 a pôs imediatamente antes da M26-F06, porque o Codex vira provider do ponto único e precisa do perfil isolado antes. A **M26-F06** ([#256](https://github.com/RodReis/rrb-jarvisOS/issues/256), PR [#269](https://github.com/RodReis/rrb-jarvisOS/pull/269), mergeado em `1067f65`) fechava o MVP-026 com seis fatias; a **M26-F07** ([#274](https://github.com/RodReis/rrb-jarvisOS/issues/274), PR [#278](https://github.com/RodReis/rrb-jarvisOS/pull/278)) nasce depois dele, da **emenda E1** à SPEC-Fases-03 aprovada em 2026-09-05 — o teste do PI com prompt pequeno mostrou o CLI gerando documento com a governança do repositório dentro (230.444 tokens de entrada; agora 1.634). São **sete fatias entregues**; o fechamento do épico [#250](https://github.com/RodReis/rrb-jarvisOS/issues/250) é ato do PI. A fila segue para o Shell (MVP-022) e o Command Center, e **a cabeça dela é decisão do PI**: nenhuma issue leva `proplan:next` até ele definir.** Histórico detalhado em `docs/STATUS-ARQUIVO.md`; o board vence divergência factual.

## Agora

| Coluna | Item | Estado |
|---|---|---|
| Feito | [#200](https://github.com/RodReis/rrb-jarvisOS/issues/200) · M17-F01 STT local e push-to-talk — **2ª e última entrega** | **entregue no PR [#338](https://github.com/RodReis/rrb-jarvisOS/pull/338)**, mergeado na `main` em 2026-09-08 (`076113c`), CI verde nos nove jobs; `proplan:done`, aguardando o aceite do PI. Fecha as quatro peças que a 1ª entrega ([PR #299](https://github.com/RodReis/rrb-jarvisOS/pull/299)) deixou: **engine faster-whisper real**, **hashes medidos**, **critério 6** (Settings) e **hotkey global**. **Os 29 hashes foram baixados e medidos**, não copiados, e conferidos contra a fonte em duas vias — o runtime contra o `SHA256SUMS` do release **e** o digest da API do GitHub, o `model.bin` contra o oid LFS do Hugging Face, as 24 wheels contra o digest que o PyPI publica por arquivo. Tudo pinado por **revisão imutável** (tag, commit, URL de arquivo): hash pinado contra alvo móvel passaria a recusar o download no dia em que o upstream publicasse qualquer coisa, e o usuário leria "falha de integridade" sobre arquivo legítimo. **Duas premissas do catálogo anterior estavam erradas, e só o download real mostrou:** o modelo são **quatro** arquivos (sem `config.json` e `tokenizer.json` o diretório é inválido, e a falha apareceria como erro do runtime, longe da causa) e as wheels são **24**, não uma — 84 MB de fecho resolvido pelo pip, em JSON e não em `.ts`, porque lista gerada não deve virar código a revisar linha a linha. **Dois defeitos achados pelo smoke com fala de verdade** (TTS pt-BR do Windows, 16 kHz mono), ambos invisíveis para dublê: (1) o stdout do Python no Windows assume **cp1252**, e a primeira palavra acentuada quebra a linha JSON — em português isso é toda transcrição; (2) **a queda para CPU não cobria a inferência** — o `WhisperModel` **constrói com sucesso** em `device="cuda"` numa máquina com GPU mas sem as bibliotecas CUDA e só estoura na primeira transcrição (`cublas64_12.dll is not found`), então o critério 7 ficava furado exatamente na máquina que ele existe para atender, e o erro chegava como **texto vazio**, desfecho `ok` que a tela não distingue de silêncio. A correção materializa os segmentos dentro do `try`, porque o `transcribe` devolve gerador preguiçoso e deixar a iteração para o chamador põe o erro fora do bloco que existe para pegá-lo. **Medido:** 5,30 s de áudio em **2,79 s** na 1ª chamada e **0,91 s** na 2ª (modelo já carregado), CPU `int8`. **Hotkey e modelo são listas fechadas, e isso é segurança:** o atalho é global e intercepta a tecla no sistema inteiro — campo livre deixaria o renderer sequestrar `Control+C` para a máquina toda. **Limites declarados:** o catálogo cobre **Windows x64** apenas; só o `small` tem artefatos pinados, então trocar de modelo em Settings oferece a opção mas o download recusaria por falta de entrada; **critérios 5 e 9 continuam do PI** (falar ao microfone no app e medir a latência dele) |
| Em Andamento | [#333](https://github.com/RodReis/rrb-jarvisOS/issues/333) · `[MVP2][INFRA]` Geração de documentos: abas para documento e cortes, scroll menor e uma ação por lugar | **as três etapas de geração em abas**, branch `feat/geracao-em-abas`, **PR [#336](https://github.com/RodReis/rrb-jarvisOS/pull/336)** aberta — ela carrega também a [#337](https://github.com/RodReis/rrb-jarvisOS/issues/337), achada pelo PI ao testar esta entrega. Levantado pelo PI ao testar `novo-teste`: *"scroll muito grande vertical para um projeto pequeno desse"*. **O número que abriu a issue foi medido, não estimado:** a etapa de arquitetura dava **4.941px numa janela de 900** — cinco telas e meia de rolagem. A suíte de tela não tinha como discordar: jsdom não tem layout, e `scrollHeight` ali é sempre zero; a medida exigiu galeria de prova nova (`GaleriaDaArquitetura`). **Três quartos da altura eram os quatro documentos empilhados; outra parte era duplicação** — cada proposta da IA aparecia acima das seções e de novo no corpo do documento, a mesma frase duas vezes com o botão `Cortar` nas duas. **Em ambos os painéis havia um teste afirmando isso como esperado**, com `toHaveLength(2)` e o comentário explicando as duas ocorrências: o defeito documentado como regra. **A forma entregue** (escolha do PI entre três opções apresentadas): uma aba por documento, mais **A revisar**, que reúne o que pede decisão. O DS esvazia o painel inativo, então a altura cai de verdade — nenhuma aba passa de **1.665px**, e três das cinco cabem na janela sem rolar. **Reunir não é misturar:** as propostas seguem agrupadas por documento e os ajustes por tipo, ambos colapsados com a contagem visível fechada. Sem isso a aba de revisão virava a pilha nova, com 2.654px medidos — a correção nasceu da própria medição. **O contador vai no rótulo da aba, não num badge:** número é texto e atravessa leitor de tela, daltonismo e escala de cinza; forma sozinha não. Sem pendência o rótulo muda de texto, porque `(0)` anuncia o que não existe. **Cada etapa tem o que lhe é próprio.** No PRD a **contradição conta no contador**, porque ela trava o aceite — omiti-la anunciaria "nada a revisar" numa tela que recusa aceitar; e ela aparece primeiro, sem colapsar. No roadmap as abas separam **etapas do trabalho**, não documentos (escolher o MVP, ler a SPEC, aprovar os gates), a aba da SPEC existe mesmo sem SPEC e diz o que falta, e o desfecho da aprovação fica **fora** das abas. **Um defeito de layout achado por captura do PI, fora do escopo da issue e corrigido junto:** o texto das afirmações usava **438px numa coluna de 832**, quebrando em duas linhas onde cabia uma — `max-w-[58ch]` a 14px vale 437px, e dentro da coluna de conteúdo ele limita duas vezes. **O brief já tinha esta correção desde a #287; arquitetura e PRD não a receberam na época** — o mesmo padrão de conserto por arquivo que deixa irmãos quebrados. Os dois corrigidos juntos. **2.331 testes verdes** (Tela 664, Regras 1667) mais **164 provas visuais**, 8 delas novas em `arquitetura.prova.ts` — o gate visual é o único que mede altura de página e largura de texto. **Contrafactuais medidos: 2** (sem o contador, quatro testes reprovam; com a duplicação de volta, o teste do corte reprova). **Correção de uma afirmação minha registrada no commit:** o Radix **não** desmonta o elemento `tabpanel` inativo — ele mantém os cinco no documento e esvazia o conteúdo. O efeito na altura é o mesmo, e a prova mede filhos e altura em vez de contar elementos |
| Em Andamento | [#337](https://github.com/RodReis/rrb-jarvisOS/issues/337) · `[MVP2][FIX]` Geração sem sinal: a barra de andamento só existe no PRD, e a saída do roadmap chega como JSON cru | **quatro defeitos, mesma PR da #333** (branch `feat/geracao-em-abas`). Todos achados pelo **teste manual do PI** percorrendo a jornada em `novo-teste`, e nenhum visível para a suíte. **(1) A barra de andamento só existia no PRD.** Ela nasceu na #287 com **um único call site**, e a regra é da tela da etapa — SPEC-Jornada-03 § Emenda E2. O PI gerou o roadmap e viu **58,9 segundos** de carregamento sem nada na tela. **Não bastava ligar o componente:** as etapas do PRD começam por `pesquisa`, que a arquitetura não faz, e a barra pararia em 60% numa geração terminada. Cada geração ganhou contrato próprio (`ETAPAS_DA_ARQUITETURA`, `ETAPAS_DO_ROADMAP`, `ETAPAS_DA_ESCOLHA`), e `NOME_DA_ETAPA` é `Record` completo — etapa nova quebra a compilação em vez de virar rótulo vazio. **A tela do roadmap tem duas gerações**, e o contrato troca conforme o ato: gerar propõe MVPs e checa o grafo; escolher escreve a SPEC. **A checagem de dependências é etapa própria** — o validador devolve forma e grafo numa lista só, mas um MVP incompleto e um ciclo pedem correções diferentes do PI. **(2) O roadmap e a SPEC chegavam como JSON cru no console:** `lerSaidaComoDocumento` reconhecia só a forma das afirmações do PRD, e um MVP não tem `texto` — tem `tese` e `resultado`. Verificado contra a saída real do banco do PI: **10 MVPs, três linhas cada, nenhum resto cru**, onde antes eram 8.520 caracteres de JSON. **(3) A SPEC da fatia chegava e era descartada.** Os três traços de `spec-aceita` no banco do PI terminaram **concluídos** — o modelo gastou 5.612 tokens e escreveu a especificação inteira. Ela veio como `{"spec":{"spec":{...}}}`. A causa é a soma de duas instruções corretas: o system mostra o envelope (os outros providers só têm texto) e o argumento da ferramenta `StructuredOutput` **já é** esse envelope. `SCHEMA_DA_SPEC` é o **único** envelope de objeto da jornada; nas listas o nível extra seria objeto onde o schema pede array, e o CLI recusaria antes de sair — por isso o roadmap, com o mesmo padrão, veio limpo. Corrigido dos dois lados: o leitor desaninha, e a dica do adapter deixa de pedir errado. **(4) O rótulo escondia o custo:** *"o que é isso de colocar na fila?"* — o clique grava o MVP **e gera a SPEC**, quase um minuto de modelo. Decisão do PI entre três opções: **"Escolher e gerar a SPEC"**. **Um erro meu corrigido no caminho:** eu disse que a geração da SPEC não passava `--json-schema`. Passa; a causa era outra, e só o banco a mostrou. **3566 testes verdes** (Regras 1673, Banco 1214, Tela 679) mais **169 provas visuais**, 5 novas. **A barra ganhou o gate visual que nunca teve** — ela distingue estados por forma de 8px, e nenhum teste de tela reprova um marcador a 2px. **Contrafactuais medidos: 5** |
| Feito | [#332](https://github.com/RodReis/rrb-jarvisOS/issues/332) · `[MVP2][FIX]` Anexos → Arquitetura: trilha parada, blob divergente falso, bloqueio invisível, CTA inerte e ajustes sem sinal | **entregue no PR [#334](https://github.com/RodReis/rrb-jarvisOS/pull/334)**, mergeado na `main` em 2026-09-07 (`c33f0d2a`), CI verde nos nove jobs; `proplan:done`, aguardando o aceite do PI. Todos achados pelo **teste manual do PI** percorrendo a jornada num projeto novo, e nenhum deles aparecia na suíte — os cinco vivem em costuras que nenhum teste de papel atravessava. **(1) Trilha parada:** `AnexosDeDesign` era o único painel da jornada sem callback ao pai; o marco `design-anexado` saía no main no ato do anexo e a trilha só refletia a etapa nova depois de recarregar a janela. **(2) `COMMIT DESATUALIZADO` falso:** o `maxBuffer` de 64 KB do `spawnSync` **abortava** com `ENOBUFS` num `git show` de documento de 255 KB, e o hash recalculado sobre saída truncada nunca batia. O conserto não foi levantar o teto — ele protege auditoria e tela — e sim restringir a quem se aplica: `saidaEhConteudo` ganha teto próprio de 64 MB, os `stderr` mantêm os 64 KB de evidência. **(3) Estado de bloqueio invisível:** o extrator do protótipo procurava estados só nos títulos, e o fixture do E2E declarava os dele exatamente ali — **passava com o defeito**. Jornada (nome de tela, âncora da arquitetura) e estado (sinal em texto visível) passam a ser buscas separadas; alargar `jornadas` teria sido o conserto errado. **(4) CTA inerte:** o botão da trilha prometia "Gerar a arquitetura" e só rolava a página até um segundo botão de mesmo nome. **Regra do PI:** avanço e aceite ficam na trilha; `Gerar de novo` e `Verificar de novo` ficam no painel. Os botões duplicados saíram de Brief, PRD, Arquitetura e Roadmap. **(5) Ajustes sem sinal:** a lista dos oito ajustes fica abaixo da dobra e quem acabou de clicar em gerar está olhando o topo — o PI os descobriu rolando a página. Pop-up **por decisão do PI**, com a ressalva registrada: ele anuncia, responde a pergunta que o PI fez ("ajuste é DISCARTE?") e leva até a lista, sem decidir nada. **3514 testes verdes** (Regras 1658, Banco 1209, Tela 647) mais 4 E2E de anexos. **Contrafactuais medidos: 4** — o aborto do `maxBuffer`, o estado fora do heading, a execução do CTA (2 testes) e a detecção da chegada (2 testes). **Um desvio deliberado da regra do PI:** no Roadmap os aceites são gates numa lista, cada linha com o próprio estado, e um botão fora dela não diria qual gate está sendo aprovado — só a geração migrou para a trilha. Registrado na issue para o PI decidir. **Limite declarado:** o redesenho da tela de geração em abas ([#333](https://github.com/RodReis/rrb-jarvisOS/issues/333)) fica fora — é onde a unificação dos botões do Roadmap se resolve com o desenho ao lado |
| Feito | [#287](https://github.com/RodReis/rrb-jarvisOS/issues/287) · Forma do planejamento e progresso real da geração | **entregue no PR [#288](https://github.com/RodReis/rrb-jarvisOS/pull/288)**, mergeado na `main` em 2026-09-05 (`606f7a1`), CI verde nos quatro jobs; `proplan:done`, aguardando o aceite do PI. Quatro pedidos do PI de 2026-09-05, com capturas. **Três são forma** (régua de fase no card, coluna de julgamento no brief, saída do modelo legível) — o certo já está escrito nos princípios 2 e 5 do `PRODUCT.md`. **O quarto é comportamento novo:** a porcentagem exige o serviço anunciar etapas, o que não existia no contrato de `GenerationEvent`; **não é FIX e não tem spec**, e o PI autorizou o escopo explicitamente ao ser avisado disso — a issue registra a autorização. **A decisão que governou as quatro telas veio de o PI apontar a paleta:** a proposta inicial pintava progresso com o acento do usuário, e a medição mostrou que isso quebra — `#2323FF` dá **2,58:1** sobre o carbono e `#8A00C4` **2,70:1** (invisíveis), `#FF2C2C` faria geração saudável parecer erro e `#2CFF05` faria etapa pendente parecer concluída. Pintar **estado** com a cor da preferência viola o princípio 5; quem distingue vencido de pendente passa a ser **forma** (preenchido contra vazado), legível nas oito cores e em escala de cinza. O acento fica onde é marca: a lâmina do hover e o botão primário. **O progresso conta fatos, não tempo** — cinco etapas concluídas sobre o total, nunca cronômetro, e a gravação segue o desfecho real do `persistir` (anunciar conclusão ali deixaria a barra em 100% sobre um pacote que não foi para o disco). **A leitura da saída é tolerante por construção:** durante o streaming o envelope `{"afirmacoes":[` nunca fecha, e coletar só objetos de profundidade zero devolveria lista vazia a geração inteira — o teste do streaming parcial pegou esse defeito na primeira execução. **Dois defeitos meus foram achados pelos testes existentes** (duplicação de rótulo e de pendência entre alerta e painel), e **um só pela captura**: o parágrafo ocupava **366px numa coluna de 536px**, porque o teto de `58ch` continuava valendo depois de a coluna já limitar a medida — quarta vez que gate visual acha o que a suíte de tela não alcança. **3251 testes verdes** (Regras 1483, Banco 1168, Tela 600), mais 154 provas visuais e 3 provas novas que travam a medida. Contrafactuais verificados: 3 (anúncio que estoura, varredura aninhada, medida da coluna). **Limite declarado:** o texto `1 / 1` numa fase de etapa única continua na tela — anterior a esta entrega, e corrigi-lo é decisão do PI |
| Feito | [#281](https://github.com/RodReis/rrb-jarvisOS/issues/281) e [#280](https://github.com/RodReis/rrb-jarvisOS/issues/280) · [FIX] A jornada não saía do prompt, e o schema do brief proibia as pendências | **entregue no PR [#282](https://github.com/RodReis/rrb-jarvisOS/pull/282)**; `proplan:done`, aguardando o aceite do PI. Os dois defeitos que o **teste do PI achou na primeira geração que chegou ao fim** — antes das correções #271/#272/#274 nenhuma chegava, e por isso nenhum aparecia. **#280:** o `--json-schema` do brief usava o envelope de **uma chave** (`afirmacoes`) com `additionalProperties: false`, e o system pede **duas**. O CLI recusava `pendencias` antes de o documento existir; o modelo cumpria a restrição escrevendo o que sobrou onde coubesse. As duas gerações reais gravaram `pendencias: []`, e o que deveria **bloquear o aceite** virou afirmação `proposto` no bloco de riscos — documento aparentemente completo, freio inexistente. **#281:** `eventosObservados` sustentava `prompt-salvo` e `refinamento-respondido` a partir de **uma fonte só**, as respostas do wizard do MVP-008, que o fluxo novo não grava. Projeto que escrevia o prompt, commitava o `PROMPT.md` e gerava o brief ficava em `prompt` **para sempre**, sem botão que o tirasse dali; a M25-F01 registrou a lacuna sabendo dela e a M25-F02 não a fechou. **Decisão do PI (2026-09-05):** o brief nasce no **fim do refinamento** e é ele que fecha a etapa — corrige de graça um segundo defeito, porque gerando na tela do prompt `decisoesDoRefinamento` chegava sempre vazio e **nenhuma afirmação podia ter origem `decisao`**. Um defeito latente achado no caminho: o laço de marco → evento parava na chave que batia com o `ultimoMarco`, e `estrutura-inicial` não está no mapa — ele empurrava a cadeia inteira para projeto recém-criado, e só não virava jornada adiantada por acidente. **3229 testes verdes** (+24). Contrafactuais: 2 sem o schema, 4 sem a entrada do marco, 1 sem a leitura do brief, e o E2E novo reprova sem o conserto |
| Feito | [#251](https://github.com/RodReis/rrb-jarvisOS/issues/251) · M26-F01 Fase do projeto e card completo | **entregue no PR [#258](https://github.com/RodReis/rrb-jarvisOS/pull/258)** (mergeado em 2026-09-04, `4b52668`), aguardando aceite do PI. Os sete critérios da SPEC-Fases-01. **Gate visual apresentado**: achou quatro defeitos que os testes de tela não pegaram (bloqueio com meia largura, fase repetindo o CTA, "CONSTRUÇÃO · CONSTRUÇÃO", espinha parando antes da última etapa) — todos corrigidos antes da entrega. Limite conhecido em [#259](https://github.com/RodReis/rrb-jarvisOS/issues/259) |
| Feito | [#275](https://github.com/RodReis/rrb-jarvisOS/issues/275) · [FIX] Combo fora do tema, chip do modelo quebrado e recusa sem o que o modelo disse | **entregue no PR [#276](https://github.com/RodReis/rrb-jarvisOS/pull/276)**, mergeado na `main` em 2026-09-05 (`3d12145`), CI verde nos quatro jobs; `proplan:done`, aguardando o aceite do PI. Três defeitos achados por **captura do PI**, todos invisíveis para os testes de papel. (1) `Select` e `Tooltip` chamavam `Portal` **sem `container`**: o FIX #107 foi aplicado arquivo a arquivo em `Overlays.tsx` e nunca varreu os outros `Portal` do DS, então a lista montava no `<body>`, fora do nó que carrega os tokens `--jos-*` como `style` inline, e **toda** variável resolvia vazio — sem fundo, sem raio, sem sombra, sem `z-index`; medido no navegador antes (`provedorContemPortal: false`, fundo `rgba(0,0,0,0)`) e depois (`true`, `rgb(20,22,27)`). Somado, a superfície de overlay herdava o card do protótipo (`rgba(...,.7)`), que é vidro por desenho: certo sobre o fundo do app, errado sobre conteúdo — nasce `surfaceOverlay`, composta a partir do card para acompanhar o tema em vez de virar segunda paleta. O `w-64` fixo do Popover também sai: o painel de troca de modelo declara `17rem` e vazava para fora da caixa. (2) O gatilho da troca usava `IconButton`, que fixa **44×44px** por desenho — `claude-opus-5` quebrava em "CLAUDE-"/"OPUS-5" e vazava a borda; nasce `ChipButton`, largura pelo conteúdo. (3) O modelo respondeu **em português** que o diretório já continha outro produto e que não ia sobrescrever, e a tela mostrou *"a saída do modelo não passou no validador"* — verdadeira, e escondendo a única informação útil da falha; a causa é `lerSaidaDoModelo` devolver `undefined` para toda recusa, então "veio prosa" e "o JSON quebrou" chegavam como o mesmo nada, e a tela descartava `problemas`, que a ponte já entregava. `lerSaidaDoModeloDetalhada` nomeia a recusa (o parser continua **sem consertar nada**) e o desfecho carrega `textoDoModelo`. Comportamento correto já escrito: `provider.tsx` § `useContainerDeOverlay`, `PRODUCT.md` § Brand Personality e PRD §14 — correção de bug documentado, sem spec. **Contrafactual verificado** no teste do portal. **O CI achou o quarto:** a guarda anti-drift barrou com zero falhas de teste — o PR altera teste e a prova 3 exige carimbo em `reports/TESTS.md`. Em andamento |
| Feito | [#284](https://github.com/RodReis/rrb-jarvisOS/issues/284) · `[FIX]` `StructuredOutput` chamado duas vezes cola `{}` no documento | **entregue no PR [#285](https://github.com/RodReis/rrb-jarvisOS/pull/285)**, mergeado na `main` em 2026-09-05 (`744f429`), CI verde (`test`, `changes` e `gate`; `e2e` pulado por não tocar a fronteira); `proplan:done`, aguardando o aceite do PI. Achado por teste do PI, e o mais enganoso da série: a tela dizia *"a detecção de contradições não devolveu saída"* com as **cinco contradições completas visíveis no Console da geração**, na mesma tela. O CLI chamou `StructuredOutput` **duas vezes** — a primeira com `input` vazio —, o parser emitia um delta por chamada, e os dois colavam em `{}{"contradicoes":[…]}`, que nenhum `JSON.parse` aceita. O serviço então tratava a saída como falha da chamada e barrava o aceite, **corretamente**: a distinção entre lista vazia e `undefined` está certa e não foi tocada. Evidência lida de `generation_trace_event` (dois eventos `texto`, um com 2 caracteres e outro com 4030), não reconstruída. Conserto no **parser**, onde as **sete** chamadas com `jsonSchema` passam — consertar só o PRD repetiria o modo de falha da M26-F06 e do FIX #275. Comportamento correto já no `ARCHITECTURE.md` § Providers: correção de bug documentado, sem spec. Contrafactual verificado revertendo a guarda. **Limite declarado:** duas chamadas ambas com conteúdo continuariam colando — não observado, e resolvê-lo é decisão do PI. **O CI achou um instável que não é desta entrega:** `console-da-geracao.test.tsx` > "geração nova limpa a trilha da anterior" reprovou uma vez com `TypeError: … reading 'generationHistory'`. O **mesmo commit de código** passou o arquivo com 21 testes no run anterior — entre os dois só mudou `reports/TESTS.md`, que nenhum teste importa —, e o console não aparece em `git diff --name-only main...HEAD`. É corrida real: o `useEffect` tem `traceAoVivo` nas dependências, então trocar de geração redispara a leitura de `window.jarvis.generationHistory`, e quando ela cai depois do `afterEach` a ponte já foi removida — só sob a carga do runner. Reproduzido em miniatura com a mensagem idêntica. `rerun --failed` desbloqueia e **não** conserta; vale FIX próprio, a critério do PI |
| Feito | [#289](https://github.com/RodReis/rrb-jarvisOS/issues/289) · `[FIX]` O CLI repete a saída estruturada e os dois documentos colam | **entregue no PR [#292](https://github.com/RodReis/rrb-jarvisOS/pull/292)**, mergeado na `main` em 2026-09-05 (`1113a14`), CI verde; `proplan:done`, aguardando o aceite do PI. É o **limite que a [#284](https://github.com/RodReis/rrb-jarvisOS/issues/284) declarou** — e que o CLI provoca por desenho, então ia se repetir sozinho. Quando ele **não reconhece** a chamada de `StructuredOutput`, injeta `[structured-output-enforce] You MUST call the StructuredOutput tool` e o modelo repete o documento: na geração real de 21:32, dois deltas de **3641 bytes byte a byte idênticos**, com o lembrete entre eles. Cada delta sozinho parseia; só a soma quebra, na posição 3641. **Decisão do PI (2026-09-05): vale a última chamada** — é o que o próprio CLI sinaliza ao exigir a rechamada, cobre os dois casos vistos e segue correto se as duas divergirem. O parser **retém** o documento e o adapter o entrega no fechamento do stdout; a união `GenerationEvent` **não** ganhou tipo novo (é fechada pela spec), o documento sai como `texto` igual a antes e só muda **quando**. **Não custa streaming, e isso foi medido:** nos traços, toda geração com `--json-schema` tem 100% dos deltas em JSON e nenhuma sem schema tem algum — os dois caminhos nunca se misturam, como o `ARCHITECTURE.md` § Providers já afirmava. **O teste que prova vive no int-spec, montado:** o parser pode reter certo e o documento nunca sair se o adapter não o entregar — a peça certa sozinha não prova a entrega. Contrafactual verificado: revertendo a retenção, o teste montado falha com a mensagem que o PI viu na tela. Um teste existente foi **atualizado, não afrouxado** — a garantia que ele protegia (não virar `ferramenta-inicio`) continua afirmada. **Nota de processo:** o PR #290 foi fechado e substituído; o branch nasceu de `main` desatualizada e carregava os commits da #288, mostrando 22 arquivos para uma correção de 4 |
| Feito | [#301](https://github.com/RodReis/rrb-jarvisOS/issues/301) · `[FIX]` O shell não preenche a janela: o rodapé para no meio | **entregue no PR [#302](https://github.com/RodReis/rrb-jarvisOS/pull/302)**, mergeado na `main` em 2026-09-06 (`d9ae46e`), CI verde; `proplan:done`, aguardando o aceite do PI. Achado por **captura do PI** com o app rodando: o rodapé parava no meio da tela, com ~280px de faixa vazia. **Quebra na cadeia de altura** — `html`/`body`/`#root` declaram 100% e o shell pede `h-full`, mas o `div` do `ProvedorDeTema`, **entre os dois**, não tinha altura; `h-full` resolve contra o pai, e pai automático o reduz ao conteúdo. **A primeira tentativa regrediu outra tela, e isso é o mais instrutivo do card:** `height: 100%` no provider consertou o shell e **empurrou os cards da CHOICE para o topo** (medido: `top: 32` contra `194` do centrado), porque cada card **reabre** o provider e ali o `div` esticou dentro de um contêiner que pedia `items-center`. Consertar um lugar e quebrar outro é o sinal de que a caixa não devia existir: `display: contents` tira o `div` da formatação e os filhos viram filhos diretos do pai real, com as variáveis herdando normalmente — **herança não depende de caixa**. **Verificado que não quebra os overlays:** o mesmo nó é o container de portal do FIX #107, e `display: contents` muda formatação, não existência; as 155 provas visuais passam. **Nenhum teste de papel pega isto** — 2165 verdes com o defeito presente, e a prova visual do shell já media as alturas de topbar e rodapé (corretas), faltava medir se o conjunto **preenche** a janela. Duas provas novas: o rodapé encosta no fim, e o provider não vira caixa. **Método que faltava:** medir no app real exige `--remote-debugging-port` e `connectOverCDP` — navegador externo não serve, porque sem a ponte com o main o app fica na tela de carregamento |
| Feito | [#304](https://github.com/RodReis/rrb-jarvisOS/issues/304) · `[FIX]` Com schema, o texto do modelo cola no documento estruturado | **entregue no PR [#305](https://github.com/RodReis/rrb-jarvisOS/pull/305)**, mergeado na `main` em 2026-09-06 (`c11c507`), CI verde; `proplan:done`, aguardando o aceite do PI. Achado pelo PI gerando o PRD: *"A detecção de contradições não devolveu saída"* nas duas tentativas, com o documento inteiro no console. **A mesma colagem `{…}{…}` da [#289](https://github.com/RodReis/rrb-jarvisOS/issues/289), por uma causa que aquela entrega declarou impossível:** o primeiro documento veio como bloco **`text`**, não como `tool_use` — o system pede "responda somente com JSON" e o modelo obedece em texto; o CLI não aceita texto como saída estruturada, injeta o `enforce` e o modelo repete pela ferramenta. A retenção do #289 só segura `tool_use`. Evidência: `texto` 3735 B → `enforce` → `texto` 3735 B idêntico no traço das contradições; e 17 KB de JSON **com quebras de linha** no traço cancelado dos documentos — `JSON.stringify` do `input` nunca produz isso. **Efeito colateral que explica o outro sintoma:** o documento nascia duas vezes, e foi isso que estourou os 120 s na primeira tentativa dos documentos (89 s numa geração só). **Duas peças, ambas no adapter:** com `jsonSchema`, texto do modelo fica guardado e só vira documento se nenhuma chamada chegar; e o system ganha a dica *"Entregue o JSON chamando a ferramenta StructuredOutput"* — mora no adapter porque a ferramenta é detalhe deste CLI, e os outros providers seguem precisando do "responda em JSON". **Medido com o CLI real pelo adapter montado:** sem a dica, texto → enforce → chamada (2955/955 tokens em duas voltas); com ela, chamada direta (1261/676). O primeiro experimento, com `spawn` cru e `shell: true`, saiu inválido — o system chegou mangled e mediu 17.735 tokens de entrada; a medida só vale pelo adapter. Int-spec montado com a sequência real falhava com a mesma `SyntaxError` da tela. `ARCHITECTURE.md` § Providers corrigido |
| Feito | [#307](https://github.com/RodReis/rrb-jarvisOS/issues/307) · `[FIX]` Contradição do PRD vira pergunta no pop-up da M8-F03, e a resposta regera os documentos | **entregue no PR [#308](https://github.com/RodReis/rrb-jarvisOS/pull/308)**, mergeado na `main` em 2026-09-06 (`1862031`), CI verde nos quatro jobs (o E2E rodou, porque a ponte do preload mudou); `proplan:done`, aguardando o aceite do PI. Achado pelo PI gerando o PRD: as contradições apareciam como **cartões só de leitura**, sem lugar para responder — um formulário vertical para decidir sozinho. **A spec já mandava outra coisa:** SPEC-Jornada-03 § Geração item 5 diz que contradição *"vira pergunta ao PI com recomendação (M8-F03)"*, e o contrato da M8-F03 é o pop-up com 2–3 opções, recomendada primeiro, impacto, texto livre e "Decide por mim". As quatro decisões do PI de 2026-09-06 (regerar os três documentos inteiros como revisão candidata; resposta gravada como `Decision` pelo mesmo canal do refinamento; regerar sozinho ao responder a última; o aceite continua sendo o clique do PI) entraram como **emenda E1** na spec. **Desenho:** `ContradicaoDoPrd` passa a **ser** uma `Pergunta`, então a máquina do `wizard.ts` e o pop-up do refinamento (`WizardDoProjeto` com `fonte`) a conduzem sem cópia; a mecânica de responder sai copiada do `RefinamentoService` para `montarDecisao` no domínio; o `PrdService` dá id próprio à contradição, valida o contrato **antes** de gravar (fora dele a etapa falha, fail closed) e leva as respostas ao pedido de geração pelo mesmo campo `decisoes`. **Medido com o CLI real:** duas contradições, três opções cada, recomendada entre elas, válidas no contrato, 19 s. **A revisão de dois eixos mudou duas coisas:** a regeração automática só roda com termo que o PI confirmou nesta sessão (reaberta a tela, o campo traz o termo re-proposto pela IA, e a pesquisa não roda sem confirmação — critério 3), e a lista inline nomeia a opção recomendada. **Limites declarados:** regeração parcial fica para a próxima entrega; a rodada de correção regera os documentos antes de detectar de novo; a moldura de gravar e auditar a decisão segue repetida nos três serviços |
| Backlog | [#200](https://github.com/RodReis/rrb-jarvisOS/issues/200) · M17-F01 STT local e push-to-talk — **abre o MVP-017** | **primeira de duas entregas mergeada** no PR [#299](https://github.com/RodReis/rrb-jarvisOS/pull/299) (`62727a2`, CI verde nos quatro jobs; o `e2e` rodou por tocar a ponte). Fatiada em duas por decisão do PI de 2026-09-06: dois dos nove critérios exigem o PI na frente do app (falar ao microfone e medir latência), e a fatia baixa ~100 MB de runtime — a arquitetura é revisada antes de gastar horas no Python. **Nesta entrega:** contrato `SttEngine` isolado por guarda de lint (verificada barrando um import real), sidecar que sobe sob demanda e **se recupera de morte**, download com hash pinado e auditoria nos dois extremos, três canais de ponte que não vazam processo/caminho/comando, e a tela do microfone com os três estados. **O engine composto é o ausente, não dublê disfarçado:** responde que o runtime não está instalado e a tela oferece baixar — o estado real de quem abre o app hoje, e o caminho do critério 4; os hashes vazios **falham fechado por construção**. **Três guardas do repo em ação:** a ponte tem **três** listas de contrato (a terceira exige handler por canal, e pegou um canal sem implementação), o tipo de evento de auditoria é união fechada e recusou os tipos novos, e minha primeira guarda de superfície varria a ponte inteira e acusou o terminal controlado — que expõe comando **de propósito** desde o MVP-004; reprovaria por um acerto. **Efeito da M22-F01:** registrar o módulo acendeu COMANDO e **mudou a rota inicial do app** para a voz, sem editar o AppShell — a projeção funcionando; três testes daquela fatia reprovaram, que é o papel deles. **O CI achou o que o typecheck local não pegou:** rodei só o do main e o erro estava no da web — `Button` não aceita eventos de ponteiro. O gesto de segurar entrou **no design system**: há ação cujo contrato é a duração, não o toque, e `onClick` não a expressa. **Falta na 2ª entrega:** engine real, hashes medidos, critério 6 (Settings) e hotkey global; os **critérios 5 e 9 são do PI** |
| Em Andamento | [#314](https://github.com/RodReis/rrb-jarvisOS/issues/314) · M27-F01 Política de PR e CI por projeto — **abre o MVP-027** | **as três entregas verticais concluídas**: a primeira pelo PR [#323](https://github.com/RodReis/rrb-jarvisOS/pull/323) (`7e56459`), a segunda pelo PR [#325](https://github.com/RodReis/rrb-jarvisOS/pull/325) (`266af2b`), a terceira pelo PR [#329](https://github.com/RodReis/rrb-jarvisOS/pull/329) (`83e3c13`), gate verde nas três. A **vertical 3 (critérios 13, 14 e 17)** fecha o que o ledger não sabia: ele conferia se o que chegou está bem-formado, nunca **se chegou tudo** — mesmo desenho da Prova 0 do relatório (issue #232), em que evidência incompleta concorda consigo mesma. O manifesto enumera o esperado e recusa PASS por ausência, hash vazio, job falho ou mistura de execuções; artefato de outra execução é descartado **antes** de procurar os ausentes, senão sobras de outro run "completariam" este. As métricas tornam impossível confundir zero com "não sei": nada assume "agora" para o fim que não chegou, e a soma **conta** as ausências em vez de somá-las como zero. A correlação com o CI entra no ledger pela migration 39, em coluna JSON única porque os campos são um bloco inteiro. **O dublê aprovava a idempotência sem que ela fosse observável** — devolvia sempre o mesmo número de PR, então o teste de retomada passaria mesmo com o serviço abrindo PR novo a cada run. **Limites:** `validarEvidencia` e `ci-metricas` são contratos sem ponto de montagem (ligá-los exige o perfil vir do pacote); `duracaoMs` do ledger segue `number` porque mudá-lo tocaria a tela e o banco; a Emenda E1 e o smoke real seguem fora. **A vertical 2 (critérios 5, 9, 10, 11 e 12)** conserta o limite que a vertical 1 declarou: a heurística do cabeçalho decidia "posso reescrever" por uma substring, o que **perde edição humana** em arquivo que a pipeline gerou e **sobrescreve arquivo alheio** com aquela linha no topo. Um manifesto versionado junto do workflow guarda o hash do que escrevemos, e "é igual ao que eu faria" deixa de ser confundido com "fui eu que fiz" — dois testes reprovam com a heurística antiga de volta. **Nome de check não é identidade:** `emissor` e `tentativa` entram na evidência, porque qualquer app com escrita publica um `validacao` verde, e tentativa antiga do mesmo workflow tem o mesmo nome. **E a base avançava sem ninguém olhar:** o head do PR não se move quando alguém mergeia outro PR na base, então o CI segue verde descrevendo código contra uma base que não existe; sem `strict`, esta reconferência antes da mutação é a única que existe. **O dublê aprovava a garantia sem que ela existisse** — devolvia o mesmo SHA para qualquer ref, e a comparação concordava consigo mesma. **Dois defeitos achados pelos testes:** exceção ao ler a base derrubava a entrega inteira, e o teste do adapter da M6-F04 afirmava o objeto normalizado inteiro. Detalhe em [DEVELOPMENT.md](DEVELOPMENT.md). **Limites:** o teste do critério 5 prova a política sobre um modelo com contador, não sobre o `test-report.mjs`; o perfil ainda não é lido de disco; a Emenda E1 segue fora |
| — | M27-F01 · vertical 1 (perfil, validação e gerador), registro da entrega anterior da mesma fatia | **entregue no PR [#323](https://github.com/RodReis/rrb-jarvisOS/pull/323)**, mergeado na `main` em 2026-09-06 (`7e56459`), gate verde em todos os jobs obrigatórios; `proplan:done`, aguardando o aceite do PI. **Primeira de três entregas verticais**, por decisão do PI de 2026-09-06. A fatia tem **26 critérios**, e a §11 da própria SPEC-Pipeline-01 sugere a divisão em perfil/gerador, integração/preservação e evidência/retomada; as 26 numa PR só produziriam um diff que ninguém revisa. A **#200 voltou ao Backlog** na mesma decisão, para manter o WIP de uma fatia por vez. **Nesta entrega (critérios 1, 2, 3, 4, 6, 7, 8 e 18):** o contrato do `ci-profile.json` com validador que devolve **todos** os problemas (ciclo por DFS tri-estado, que não acusa losango), o gerador a partir do perfil, os dois perfis de stack que a R2 exige (Node/npm e Python/pip em Windows/PowerShell), a representação `legacy` e o bloqueio da entrega quando o perfil é inválido — **antes** de escrita, push ou chamada à origem, que é a letra do critério 3. **Um defeito do gerador legado que este não repete:** o docblock dele afirma que quebra de linha "entra escapada pelo mesmo mecanismo" das aspas simples; **não entra** — medido gerando o arquivo, o YAML sai partido em duas linhas e não faz parse. Aspas simples protegem contra o *shell*; a ameaça é do *formato*. **E a terceira ameaça do critério 18:** `${{ ... }}` é expandido pelo GitHub antes de existir shell, então aspas simples são texto para o expansor. **Duas decisões de desenho:** o `needs` do agregado inclui todo grupo mas a condição só exige os obrigatórios (espera o opcional terminar em vez de ignorá-lo), e `if: always()` porque job vermelho deixaria o context **ausente**, e ausência não é falha para a proteção da branch — o gate esperaria um check que nunca chega. **Limites declarados:** o **smoke real** contra dois repositórios GitHub de teste, que a §11 pede, fica `not_run` (sem credencial nem orçamento de Actions autorizados); a distinção entre workflow gerado e humano usa a marca do cabeçalho, mais fraca que o diff de adoção da §6; o perfil ainda não é lido de disco nem entra no pacote aprovado; e a **Emenda E1** (critérios 19 a 26, preflight do `SLICE_ENTRY`) não entra aqui, porque depende do perfil existir |
| Feito | [#232](https://github.com/RodReis/rrb-jarvisOS/issues/232) · `[INFRA][FIX]` O relatório recusa execução que perdeu arquivo de teste | **entregue no PR [#297](https://github.com/RodReis/rrb-jarvisOS/pull/297)**, mergeado na `main` em 2026-09-06 (`86e6a2d`), CI verde; `proplan:done`, aguardando o aceite do PI. Puxada por decisão do PI de 2026-09-06, com o board sem `proplan:next` — a issue nasceu em Backlog em 2026-09-02 e o STATUS registrava que ela "não entra na fila por si". **A investigação derrubou duas premissas da própria issue.** (1) O gatilho nomeado, `fronteira.int-spec.ts`, supostamente *"spawn de ESLint com timeout de 5s"*, **não faz spawn** — usa a API em processo e leva 672 ms sob a suíte cheia; quando o defeito apareceu na sessão, esse teste **passou** e o worker morreu em outro lugar. (2) A direção proposta (comparar executados contra coletados) **não tem fonte**: o JSON diz quantos arquivos o runner relatou, nunca quantos pretendia rodar, e `numTotalTestSuites` conta blocos `describe` — **379 contra 67** arquivos no `banco` —, então guardá-lo acusaria toda execução saudável. **A correção é um piso por categoria** em `reports/.arquivos-por-categoria.json`: cair abaixo dele recusa gravar e sai com código 1. É a **Prova 0** do `--check` e vale também fora dele, porque as outras três auditam o *conteúdo* do relatório e esta audita a *evidência que o alimenta* — sem ela, a guarda anti-drift compara o relatório contra a mesma execução incompleta e **concorda consigo mesma**. Arquivo próprio e não coluna, porque o histórico é append-only e coluna nova reescreveria toda linha passada. **Verificado com perda simulada** num JSON real (66 de 67 arquivos, zero falhas, `success: true`): recusa gravar e o relatório fica intacto; sem a guarda, o número falso (1181 contra 1186) entra sem nada acusar. **Limite declarado:** fecha o **modo de falha**, não a causa — por que o worker morre não reproduz sob demanda, e fixtures que o matam de propósito saem com código zero porque o Vitest 4.1 se recupera |
| Feito | [#206](https://github.com/RodReis/rrb-jarvisOS/issues/206) · M22-F01 Menu do JARVIS OS e do Agents OS projetado do registro | **entregue no PR [#296](https://github.com/RodReis/rrb-jarvisOS/pull/296)**; `proplan:done`, aguardando o aceite do PI. Os nove critérios da SPEC-Shell-01, e a **primeira fatia do MVP-022** — puxada com o board sem `proplan:next`, por decisão do PI de 2026-09-05 entre as três opções abertas. A sidebar deixa de ter lista de itens: renderiza o que o **registro de módulos** devolve, agrupado e ordenado, e é isso que faz a fatia que entrega uma tela acender o item dela **sem editar o AppShell**. Visível hoje: NEGÓCIOS › Projects Hub · SISTEMA › Terminal, Settings no Professional Ops; GOVERNANCE › Operator Central no Agents OS — pouco, e a spec já diz que é **verdade**. **Uma decisão do PI antes de codificar:** o `Settings` era um ramo `if` antes do mapa de módulos, e a exceção obrigaria o teste do critério 2 (*"nenhuma rota visível cai no placeholder"*) a abrir brecha justo para ele — exceção no teste que guarda a garantia da fatia enfraquece a garantia, e a própria `navegacao.ts` avisa que "caso especial é onde vazamento se esconde"; ele entra no registro como qualquer módulo. **Rotas renomeadas:** `operacoes` → `operator` (a fila de aprovação é **governança**, não "Operações", que no protótipo é Kanban/Workflows) e `projetos` → `projects`; `inicio` e `agentes` **saem**, e a rota inicial passa a ser o primeiro item visível. **Contrafactual medido:** um item sem tela no registro (`Kanban`) reprova **cinco** testes, dois deles de tela — a projeção é medida, não presumida. O critério 4 traz o contrafactual que a spec pede: um executor a mais vira um item a mais, onde tabela fixa devolveria seis nos dois casos. **Testes dos vizinhos atualizados, não afrouxados:** a rota inicial passou a montar a lista de projetos, então quatro dublês da ponte ganharam o que a tela pede, e o teste de isolamento por espaço troca "Operações" por "Terminal" — mesmo papel, rota do Professional Ops diferente da inicial. **Limites declarados:** (1) `migrarRota` **não tem ponto de ligação hoje** — a rota não é persistida em disco nem pelo `usePreferences` nem pelo main, então não há estado antigo a migrar; a função é o contrato do critério 6 e o ponto de ligação de quando a persistência existir. (2) **Sem E2E do menu:** sem consentimento do Google o AppShell nunca monta, mesmo limite já declarado em `projetos-locais.e2e.ts` e recusado em `login.e2e.ts`. (3) Verificação visual contra os mockups pendente do PI |
| Feito | [#283](https://github.com/RodReis/rrb-jarvisOS/issues/283) · `[FIX]` Nenhum teste mede o `SIGKILL` do corte por ferramenta | **entregue no PR [#294](https://github.com/RodReis/rrb-jarvisOS/pull/294)**, mergeado na `main` em 2026-09-05 (`67a4b8f`), CI verde; `proplan:done`, aguardando o aceite do PI. Banco **1172** (+2). O critério 3 da emenda E1 pede **duas** garantias — *"evento `erro` no console e a geração termina; o documento não recebe o texto posterior"* — e só a segunda tinha teste. A flag `ferramentaProibida` protege o **documento** e já era medida; o `processo.kill(SIGKILL)` protege **custo e tempo** e não era medido por nada: trocá-lo por um no-op deixava os 39 testes do arquivo verdes. Sem o kill, a sessão que saiu do contrato segue rodando, consome a assinatura e segura a geração até o timeout, com toda a saída descartada no fim — e quem o removesse numa refatoração não veria teste reprovar. **Por que faltava:** o dublê do bloco existente **termina sozinho**, e o comentário dele até explicava o porquê (um dublê vivo faria o caso da Construção reprovar por timeout) — mas é exatamente isso que torna o kill invisível. O bloco novo traz um dublê que fica vivo 4s, com o `timeoutMs` do adapter no **dobro** disso: assim terminar cedo só pode ser o corte, nunca o relógio. **Contrafactual medido:** 4052 ms sem o kill contra o limite de 2000 ms. **O segundo teste é o contrapeso** — na Construção a ferramenta é legítima e a sessão não é morta —, senão "matar sempre" passaria no primeiro e quebraria a Construção. Nenhuma mudança de comportamento: o adapter não foi tocado. **O relatório precisou ser regerado:** a primeira execução gravou Banco em 1151 aprovados, contaminada pelo worker morto do pool ([#232](https://github.com/RodReis/rrb-jarvisOS/issues/232)); a execução limpa dá 1172, igual à suíte rodada isolada |
| Finalizado | [#259](https://github.com/RodReis/rrb-jarvisOS/issues/259) · [FIX] Aceite documental é desfeito pelo recálculo | Achado na M26-F01. `brief-aceito` e `prd-aceito` não deixam evidência que `eventosObservados` enxergue, então `etapaDerivada` para em `prd-aceito` e **nenhum projeto real alcança Especificação nem Construção**. Comportamento correto na SPEC-Jornada-01, critérios 1 e 2 — correção de bug documentado, sem spec nova. **Correção decidida pelo PI em 2026-09-04: marco documental para os dois aceites.** Em andamento |
| Feito | [#252](https://github.com/RodReis/rrb-jarvisOS/issues/252) · M26-F02 Catálogo e modelo por fase | **entregue**, aguardando aceite do PI. Os oito critérios da SPEC-Fases-02. Achou **dois defeitos silenciosos**: `resolverProvider` descartava `request.model` (a escolha do PI não teria efeito, e o ledger registraria o modelo errado sem erro nenhum), e o `modeloAtivo` do card divergiu da geração assim que a jornada passou a usar `modeloDaFase`. Troca por projeto no selo do card — decisão do PI, porque a F01 entregou o selo ali e não no projeto aberto |
| Finalizado | [#253](https://github.com/RodReis/rrb-jarvisOS/issues/253) · M26-F03 Console da geração | **aceite do PI registrado**; entregue no PR [#263](https://github.com/RodReis/rrb-jarvisOS/pull/263), mergeado em 2026-09-04. Os oito critérios da SPEC-Fases-03. **Quatro premissas da spec não existiam no código** e foram ao PI antes de codificar: `ledgerEntryId` vira o `call_id` do ponto único (a PK do `cost_event` é descartada por `recordCost`); `Disclosure` novo no DS (não havia collapse); generalizar o coletor de retenção (que era monolítico e **nunca fora instanciado**); canal IPC único com `traceId` no payload. **Nove defeitos achados, todos com contrafactual verificado.** Os dois graves: o **segredo chegava ao disco** (`redact` age sobre nome de campo; `Bearer sk-ant-…` numa linha de `Bash` não tem campo a reconhecer — nasce `redigirSegredos`), e a **redação não valia ao vivo** (ia redigida ao banco e em claro para a tela; pela letra do critério 3 não era violação, pelo ADR-005 era — `eventoSeguro` move para a entrada do coletor). O E2E também achou que o **critério 7 não tinha lastro**: `anthropic`, `gemini` e `ollama` não têm uma linha de `onEvento`, então gravavam trace **vazio** — os testes que o carimbavam chamavam `registrar` à mão, dublê complacente. O **gate visual** achou o rótulo em MAIÚSCULAS (`LABEL_MONO` carrega `uppercase`, e ali o rótulo é conteúdo). **Ganho de graça:** o `usage` deixa de ser aproximado por caracteres — o CLI o reporta em `stream-json`, e o smoke real mediu **230.444 tokens de entrada** contra os ~30 da divisão por 4. Limite conhecido: a expiração de **anexos** do coletor continua sem rodar (precisa do layout do diretório, escopo da M9-F06) |
| Finalizado | [#254](https://github.com/RodReis/rrb-jarvisOS/issues/254) · M26-F04 Marcos Git e gate da construção | **aceite do PI registrado**; entregue no PR [#264](https://github.com/RodReis/rrb-jarvisOS/pull/264), mergeado em 2026-09-04. Os seis critérios da SPEC-Fases-04. **Uma premissa da spec não fechava e foi ao PI antes de codificar:** o critério 1 pede `hash da revisão = git hash-object do arquivo no commit`, e a igualdade é **impossível de fato** — os hashes gravados são SHA-256 do texto e `hash-object` é SHA-1 de blob com header, então o gate **nunca abriria**. Decisão do PI (2026-09-04): ler o conteúdo como está no commit (`git show <sha>:<caminho>`) e recalcular o SHA-256 do mesmo jeito que a gravação calculou. **O smoke com Git real achou o defeito que 25 testes verdes escondiam:** repositório **sem commit nenhum** era tratado como Git indisponível — o estado exato de um projeto recém-criado, porque `createProject` roda `git init` e o primeiro commit só vem com o marco `estrutura-inicial`; o painel acusaria problema de ferramenta no caminho mais comum que existe. A distinção certa é o `exitCode`, não o `reason`: o `TerminalEngine` classifica **toda** saída não-zero como `falha-na-execucao` (a mesma etiqueta de um `npm test` que reprova), e só um processo que não chegou a rodar fica com `exitCode: null`. Com dublê, `HEAD` sempre respondia — sexta vez no projeto que a infraestrutura real acha o que o dublê esconde. **`verificarMarcos` é obrigatória em `RoadmapDeps`** de propósito: um campo opcional teria duas leituras indistinguíveis em runtime ("não verifica" / "esqueceram de ligar"), e a segunda abriria a Construção sem verificação nenhuma. **Nenhum canal de escrita novo** — "Commitar marco" reusa `completeMilestone`, e o E2E prova a ausência pela **superfície** da ponte, não por asserção sobre o que a tela chama. **O gate visual achou dois defeitos** que nenhum teste de tela pega: o aviso de Git indisponível media **3,90:1** no modo claro (abaixo da régua de 4.5:1 do PRD §14, justo na mensagem mais importante da tela) — passa a `InlineAlert`; e a **coluna do hash não alinhava** entre linhas (x≈613 numa, x≈358 na seguinte), porque o `flex-1` do caminho a empurrava — é a coluna que o PI compara com o `git log`, e a correção é `subgrid` herdando as trilhas da lista, com o alinhamento travado por teste. O CI achou o terceiro e o quarto: `npm run lint` inclui `prettier --check .`, que o lint por arquivo não cobria, e a guarda anti-drift com `reports/TESTS.md` desatualizado. **A revisão de código achou o quinto, e era o mais grave: o conteúdo do documento ia para a auditoria de comandos.** `git show <sha>:<caminho>` devolve o arquivo inteiro no `stdout`, e o `TerminalEngine` persiste `stdout` em todo `AuditEvent` — era o primeiro comando desta base cujo propósito é despejar conteúdo, e o `redact()` do ADR-004 não alcança (age sobre nome de campo; documento não tem campo). O próprio código da fatia declarava a garantia em dois comentários e não a cumpria na camada de baixo. Corrigido com `CommandSubmission.saidaEhConteudo` — o `stdout` vira marcador na auditoria, o chamador segue recebendo a saída real, e o resto do rastro continua; contrafactual verificado. Limite declarado: o fluxo *bloqueado → commitar → aceite* exige sessão autenticada e o harness não consente Google (mesmo limite de `roadmap-aprovacoes.e2e.ts`); é provado no int-spec com identidade injetada. **Gate visual apresentado** (14 provas × 2 modos, 3 cenas) |
| Feito | [#117](https://github.com/RodReis/rrb-jarvisOS/issues/117) · M10-F02 Autenticação e perfil isolado do Codex | **entregue no PR [#267](https://github.com/RodReis/rrb-jarvisOS/pull/267)**, mergeado na `main` em 2026-09-05 (`45d069c`), CI verde nos quatro jobs; `proplan:done`, aguardando o aceite do PI. Cinco dos seis critérios da SPEC-Multi-Executor-02; o **critério 2 fica declarado como limite**, pela emenda do PI de 2026-09-04 (o mount no container depende da M10-F01). **Três decisões do PI antes de codificar:** login por `--device-auth` (as alternativas `--with-api-key`/`--with-access-token` leem do **stdin**, o que faria o segredo passar por dentro do processo do app); **`quota_unknown` fixo e modo declarado pelo PI, nunca inferido** — medido no CLI 0.149.0, os 24 checks de `codex doctor --json` não reportam quota, uso, rate limit nem crédito (zero ocorrências), então é o que a regra 2 manda; e critério 2 como limite declarado, com o isolamento provado no host. **O smoke real achou três defeitos que o dublê escondia** (sétima vez no projeto): (1) no Windows o `codex` do npm existe **só como `.cmd`**, e o Node recusa executá-lo com `shell: false` (`EINVAL`, proteção contra injeção em scripts de lote) — o serviço responderia `offline` numa máquina com o Codex funcionando; decisão do PI: resolver `bin/codex.js` via `npm root -g` e chamar pelo Node, mantendo `shell: false` intacto; (2) `login status` escreve **`"Not logged in"` no `stderr`**, com exit 1 — ler só o stdout faria perfil sem credencial parecer autenticado, e o run morreria na primeira chamada depois de o estado ter dito que estava tudo bem; (3) o CLI **exige que o `CODEX_HOME` já exista**, e em produção o perfil nasce vazio, então a fatia inteira falharia no primeiro uso. **O gate visual achou** que o `CODEX_HOME` é uma palavra só, longa, dentro de um `flex-wrap` — que quebra entre itens, nunca dentro de um token sem espaços; mesma correção do SHA de 40 caracteres da M26-F04, travada por teste. **O lint achou um antipadrão real:** `setState` dentro de efeito para limpar a instrução, que virou valor derivado no render. **E o CI achou o quarto, por erro de recorte meu:** verifiquei a formatação só dos arquivos **não commitados** (`git status`), quando `npm run lint` roda `prettier --check .` no repositório inteiro — três arquivos da fatia já estavam commitados e saíram do recorte. O recorte certo é `git diff --name-only main...HEAD` |
| Finalizado | [#255](https://github.com/RodReis/rrb-jarvisOS/issues/255) · M26-F05 Modelo da fase no run | **aceite do PI registrado**; entregue no PR [#265](https://github.com/RodReis/rrb-jarvisOS/pull/265), mergeado na main em 2026-09-05 (`ace36e4`), CI verde nos quatro jobs. Os seis critérios da SPEC-Fases-05. **Uma premissa da spec não fechava e foi ao PI antes de codificar:** o critério 4 pede recusa "antes de criar container" e o § Dentro diz que o `ConstrutorService` resolve "no início do run" — impossível juntos, porque o preflight roda antes. Decisão do PI (2026-09-04): **o preflight resolve, valida e congela; o construtor só lê**. Segunda pergunta: `modeloDaFase` nunca devolve modelo fora do catálogo (cai no padrão do workspace, e o IPC recusa antes de gravar), então o estado do critério 4 só nasce por escrita direta no banco — decisão do PI: **implementar a barreira mesmo assim**, como `setModeloDaFase` já faz. `modeloDaConstrucao` é **obrigatória** em `PreflightDeps` pela razão da M26-F04: campo opcional teria duas leituras indistinguíveis, e a segunda mandaria todo run ao default do CLI em silêncio. **O gate visual achou um defeito que nenhum teste de tela pega:** o quinto item numa grade `sm:grid-cols-4` cairia sozinho numa segunda linha com três lacunas — o modelo saiu da grade numérica para junto do badge de desfecho, como texto e não `Badge` (que aplica `uppercase` sobre o que aqui é conteúdo, e cujos tons são todos semânticos). Contraste medido **no fundo efetivo**, compondo a translucidez de `surfaceRaised`: 7,11:1 no escuro e 13,59:1 no claro. **Limites:** smoke real nos termos da M9-F04/F06 (imagem sem o binário `claude`); o `PainelDeEntrega` não tem cena na prova visual |
| Feito | [#274](https://github.com/RodReis/rrb-jarvisOS/issues/274) · M26-F07 Isolamento do CLI por fase, com as correções [#271](https://github.com/RodReis/rrb-jarvisOS/issues/271) e [#272](https://github.com/RodReis/rrb-jarvisOS/issues/272) | **entregue no PR [#278](https://github.com/RodReis/rrb-jarvisOS/pull/278)**, mergeado na `main` em 2026-09-05 (`883969c`), CI verde nos quatro jobs; `proplan:done`, aguardando o aceite do PI. Os seis critérios da **emenda E1** da SPEC-Fases-03. Um defeito visto de três ângulos: uma geração de documento que se comportava como sessão de agente. **#271:** `generateStream` escrevia só `request.prompt` no stdin e ignorava `request.system` — `SISTEMA_DAS_PERGUNTAS` nunca chegava, e o CLI respondia na persona padrão de agente de código; os outros três adapters já o passavam, só este não. Agora `--system-prompt`, em **qualquer** fase (entregar o contrato não tem relação com ferramentas). O Codex não tem flag equivalente: o system entra antes do prompt no stdin com cabeçalhos. Os nove systems declaram o idioma — a saída em português era **imitação do prompt**, não contrato. **#272:** o parser tratava `user` igual a `assistant`, e o CLI usa `user` para injetar o que ninguém digitou; o `truncarBytes` de 2 KB só cobria `tool_result`, então a skill `claude-api` inteira entrou no documento. Agora `texto` só nasce de `assistant` e o injetado vira `ferramenta-fim` truncado — **não** `erro`, que mentiria e faria o PI ignorar os erros de verdade. **F07:** o comentário que defendia o `process.cwd()` ("o diretório do app, nunca o do usuário") estava errado nas duas metades — em dev o diretório do app **é** o repositório do JarvisOS. **O smoke real achou o que o dublê escondia:** `--json-schema` **não** faz o modelo escrever JSON em texto, ele injeta a ferramenta `StructuredOutput` e o documento vem no `input`, sem nenhum bloco `text` — sem tratar isso o documento nasceria vazio e o corte do critério 3 mataria a geração. E o **Codex tem duas fontes de contexto**: no cwd neutro mas com `CODEX_HOME` pessoal, a sessão ainda carregava plugins e hooks de `~/.codex/`; quem fecha essa porta é o `CODEX_HOME` da pipeline (M10-F02). **Critério 4 medido: 1.634 tokens de entrada** contra 230.444 — **99,3% abaixo**, teto de 10.000. Versões mínimas no `ARCHITECTURE.md` § Providers (`claude` 2.1.258, `codex` 0.149.0), cada flag conferida em `--help`. **Um quarto defeito, achado relendo o diff:** o `disponivel()` removia o diretório do run só no `close`, e o caso mais comum dele — binário não instalado — chega por `error`, que pode nunca emitir `close`; a tela sonda em laço, então acumularia um diretório por sondagem no `userData` de quem não tem o CLI. A limpeza passa a pendurar na resposta. **O quinto veio do CI:** os dois testes do console no E2E davam `saida-invalida` — o dublê do `claude` emitia ferramentas numa geração de Refinamento, e o critério 3 passou a cortar exatamente isso. O app estava certo; o dublê é que imitava um CLI que ignora `--tools ""`. Agora ele **olha os args** e nasce um teste do corte no app montado. **Limite declarado:** a emenda E1 vive no PR [#273](https://github.com/RodReis/rrb-jarvisOS/pull/273), mergeado na `main` em 2026-09-07 — a #274 a declara `aprovada-pi` e implementei contra esse texto |
| Feito | [#256](https://github.com/RodReis/rrb-jarvisOS/issues/256) · M26-F06 Codex no ponto único | **entregue no PR [#269](https://github.com/RodReis/rrb-jarvisOS/pull/269)**, mergeado na `main` em 2026-09-05 (`1067f65`), CI verde nos quatro jobs; `proplan:done`, aguardando o aceite do PI. **Fecha o MVP-026.** Os oito critérios da SPEC-Fases-06. **O defeito que a fatia teve de corrigir na raiz:** `PROVIDER_DA_ROTA` cravava `assinatura → 'claude-code'`, verdade com uma assinatura só — com duas, o PI escolheria Sol no combo da fase e **a chamada sairia pelo Claude**, sem erro nenhum e com o ledger registrando o provider errado; **sete serviços** liam esse mapa. Agora `escolherRota` devolve o provider que resolveu. **Três decisões do PI antes de codificar:** a função do boot passa a receber a **fase** (sem default — quatro dos cinco serviços são `planejamento` e o roadmap é `especificacao`, e um default faria o roadmap consultar a assinatura errada sem o compilador apontar); **parser próprio** para o Codex, porque os formatos não têm campo em comum e o Done 1 do MVP-010 exige que os dois passem no mesmo contract test sem compartilhar parser; e **catálogo só com a assinatura** — levantei ressalva sobre cadastrar preço de API agora, porque a spec diz *"preço a cadastrar na habilitação, não inventado aqui"* e um número chutado alimentaria o gate de orçamento; o PI confirmou seguir a spec. **Medição, não suposição** (o CLI 0.149.0 está instalado): o stdout do `codex exec --json` é **JSONL puro** — zero linhas não-JSON, logs todos no stderr, como o contrato do CLI declara —, os nomes dos tipos vêm da fonte (`exec_events.rs`), o `usage` medido chega no `turn.completed`, e `item.updated` **não perde texto** (o streaming token a token é do app-server, protocolo que esta fatia não usa; verificado antes de decidir ignorá-lo). **Dois defeitos que a implementação achou:** o health mediria só o binário, então um Codex instalado e sem login apareceria **online** enquanto toda geração bloqueava — um "online" que não gera é pior que um "offline"; e o motivo da opção desabilitada aparecia **três vezes**, um por modelo do Codex, o que o teste de tela mostrou e virou agrupamento. Um teste da M26-F02 dizia *"nenhum id do Codex entra nesta fatia"* e falhou ao ganhar o provider — **cumpriu o papel** de marcar o limite. **O relatório precisou ser regerado:** a primeira execução gravou Banco em 1074/1130 e a limpa dá 1118/1130 (estável em duas rodadas) — era o worker morto do pool ([#232](https://github.com/RodReis/rrb-jarvisOS/issues/232)). **E o CI achou o terceiro defeito:** um `toHaveLength(4)` no **E2E** do multi-provider, que o 5 do catálogo quebrou. Eu havia corrigido o mesmo número num teste de unidade e não procurei os irmãos — `npm run test` não roda Playwright, então o do E2E só reprova depois do push. Passa a afirmar os **nomes** dos cinco providers, o que é mais forte e não envelhece. Rodando local, ele falhou adiante no Ollama: eu tenho servidor local e o CI não, então `estado` sai da asserção dele e do `codex` — cravá-lo faz o teste reprovar em toda estação que tenha a ferramenta instalada. **Limite declarado:** o caminho feliz do smoke depende de sessão autenticada do PI; formato, argumentos aceitos e tradução são provados sem ela |
| Finalizado | [#238](https://github.com/RodReis/rrb-jarvisOS/issues/238) a [#242](https://github.com/RodReis/rrb-jarvisOS/issues/242) · M25-F01–F05 | **aceite do PI registrado em 2026-09-03**; as cinco em `proplan:finalizado`. Fechamento do épico [#237](https://github.com/RodReis/rrb-jarvisOS/issues/237) é ato do PI. Detalhe das entregas em `docs/STATUS-ARQUIVO.md` |
| Feito | [#106](https://github.com/RodReis/rrb-jarvisOS/issues/106) · M9-F06 Evidência, limpeza e continuidade | **a última fatia do MVP-009**, entregue no PR #236 (`2ac721d`); o PI declarou o MVP-009 finalizado em 2026-09-03 — aceite formal e fechamento de #100 são atos dele. SPEC `aprovada-pi` (2026-08-29, emendada em 2026-08-30). Entrega o `ExecutionLedger`, o `LimpezaService`, o coletor de retenção e o painel do estado terminal. **Decisão do PI em 2026-09-02:** o container passa a nascer com `--rm` (parar já remove, sem `docker rm` na allowlist de destrutivos), e a jornada E2E real completa **não roda** nesta entrega — o limite fica declarado |
| Finalizado | [#105](https://github.com/RodReis/rrb-jarvisOS/issues/105) · M9-F05 Revisão, CI e squash merge automático | **aceite do PI registrado**; entregue no PR #235 (`6bade77`). SPEC `aprovada-pi` (2026-08-29, emendada em 2026-08-30). Fechou a pendência da M9-F04: o `ConstrutorService` ganhou consumidor e a rota do executor passou a operar. **Três decisões do PI em 2026-09-02** (ver § Decisões do PI na M9-F05) |
| Finalizado | [#104](https://github.com/RodReis/rrb-jarvisOS/issues/104) · M9-F04 Construção/recuperação | **aceite do PI registrado em 2026-09-02**; entregue no PR #231 (`9005423`), CI verde. **Limites declarados:** correlação `runId`/`tentativa` do proxy fica para a M9-F05 (rota do executor não operacional em produção até lá); smoke real em `not_run` porque a imagem do sandbox não traz o binário `claude`; cancelamento cooperativo com latência até o timeout do passo. **Pendência do PI:** fail-open do `verificarEscopo` quando o `git status` do container falha |
| Feito | [#209](https://github.com/RodReis/rrb-jarvisOS/issues/209) · correção do EffectJournal | entregue no PR #227 (`66623b8`); `proplan:done`, aguardando aceite do PI |
| Backlog | [#232](https://github.com/RodReis/rrb-jarvisOS/issues/232) · `[INFRA][FIX]` worker morto no pool do Vitest | criado em 2026-09-02 na entrega da M9-F04: execução perde arquivo inteiro e ainda relata verde, o que é fechamento frágil produzido pela infra (`docs/TESTING.md` §1). Não entra na fila por si — o PI decide quando |
| Finalizado | [#222](https://github.com/RodReis/rrb-jarvisOS/issues/222) · restrição de egress do container | aceite do PI registrado; entregue no PR #228 |
| Feito | [#103](https://github.com/RodReis/rrb-jarvisOS/issues/103) · M9-F03 Worktree, preflight e Docker | entregue no PR #211; emendas e evidência preservadas |
| Finalizado | #99, #101 e #102 | aceite do PI registrado; M9-F02 integrada pelo PR #208 |
| Backlog | M7-F01–F06 (#180–#185), MVPs 010–015 e M16-F01–F04 | SPECs aprovadas; publicação documental não muda fila |
| Backlog | M16-F05/F06 (#167/#168), MVP-023 e MVP-024 | dez SPECs `aprovada-pi` em 2026-08-31; aceite não altera `next` nem inicia construção fora da fila |
| Planejado | M7-F07–F08 (#186–#187) a redigir | Fora do fechamento da V3; redação não autoriza construção |
| Roadmap preservado | Command Center (MVP-017–021) e Shell (MVP-022) | decisões e issues da main preservadas; não renumerados |

## MVPs

| MVP | Issue | Estado | Progresso |
|---|---|---|---|
| MVP-001 Fundação | [#1](https://github.com/RodReis/rrb-jarvisOS/issues/1) | fechado/aceito | 6/6 |
| MVP-002 Execução local | [#9](https://github.com/RodReis/rrb-jarvisOS/issues/9) | fechado/aceito | 5/5 |
| MVP-003 Design System | [#16](https://github.com/RodReis/rrb-jarvisOS/issues/16) | fechado/aceito | 8/8 |
| MVP-004 Execução real | [#10](https://github.com/RodReis/rrb-jarvisOS/issues/10) | fechado/aceito pelo PI em 2026-08-31 | 3/3 |
| MVP-005 Providers/Vault/Budget | [#76](https://github.com/RodReis/rrb-jarvisOS/issues/76) | fechado/aceito pelo PI em 2026-08-31 | 4/4 |
| MVP-006 Conectores Essenciais | [#86](https://github.com/RodReis/rrb-jarvisOS/issues/86) | fechado/aceito pelo PI em 2026-08-31 | 6/6 |
| MVP-007 Memória compartilhada | [#179](https://github.com/RodReis/rrb-jarvisOS/issues/179) | F01–F06 aprovadas/Backlog; F07–F08 Planejadas, fora do fechamento V3 | 0/8 |
| MVP-008 Planejamento Governado | [#93](https://github.com/RodReis/rrb-jarvisOS/issues/93) | fechado/aceito pelo PI em 2026-08-31 | 6/6 |
| MVP-009 Entrega Autônoma | [#100](https://github.com/RodReis/rrb-jarvisOS/issues/100) | seis SPECs `aprovada-pi` (2026-08-29), **emendadas pelo PI em 2026-08-30 e 2026-08-31**; F01–F05 aceitas, F06 entregue no PR #236. **PI declarou o MVP finalizado em 2026-09-03**; fechamento de #100 é ato dele | 6/6 |
| MVP-010 Multi-executor Claude + Codex | [#115](https://github.com/RodReis/rrb-jarvisOS/issues/115) | **M10-F02 ([#117](https://github.com/RodReis/rrb-jarvisOS/issues/117)) entregue** no PR [#267](https://github.com/RodReis/rrb-jarvisOS/pull/267) — puxada para antes da M26-F06 pela emenda do PI de 2026-09-04, e entregue **sem** a M10-F01 (só o critério 2, do mount no container, fica pendente dela). As demais fatias seguem no Backlog | 1/5 |
| MVP-011 Squads limitados | [#121](https://github.com/RodReis/rrb-jarvisOS/issues/121) | fatias [#122–#126](https://github.com/RodReis/rrb-jarvisOS/issues/122) no Backlog | 0/5 |
| MVP-012 Scheduler concorrente | [#127](https://github.com/RodReis/rrb-jarvisOS/issues/127) | fatias [#128–#132](https://github.com/RodReis/rrb-jarvisOS/issues/128) no Backlog | 0/5 |
| MVP-013 Execução contínua | [#133](https://github.com/RodReis/rrb-jarvisOS/issues/133) | fatias [#134–#138](https://github.com/RodReis/rrb-jarvisOS/issues/134) no Backlog | 0/5 |
| MVP-014 Release e Deploy Governado | [#149](https://github.com/RodReis/rrb-jarvisOS/issues/149) | cinco SPECs `aprovada-pi`; fatias #150–#154 em Backlog | 0/5 |
| MVP-015 Observabilidade Operacional | [#155](https://github.com/RodReis/rrb-jarvisOS/issues/155) | seis SPECs `aprovada-pi`; fatias #156–#161 em Backlog | 0/6 |
| MVP-016 Aprendizado Operacional da Pipeline | [#162](https://github.com/RodReis/rrb-jarvisOS/issues/162) | seis SPECs `aprovada-pi`; #163–#168 em Backlog | 0/6 |
| MVP-017 a MVP-021 (Command Center) | [#193](https://github.com/RodReis/rrb-jarvisOS/issues/193) · [#194](https://github.com/RodReis/rrb-jarvisOS/issues/194) · [#195](https://github.com/RodReis/rrb-jarvisOS/issues/195) · [#196](https://github.com/RodReis/rrb-jarvisOS/issues/196) · [#197](https://github.com/RodReis/rrb-jarvisOS/issues/197) | 5 épicos criados em 2026-08-30, aprovados pelo PI na mesma data (HA removido; voz local Whisper+Piper): voz+persona+mascote, escuta contínua, briefing/proatividade, integrações Gmail/Agenda/Spotify, visão Frigate. Fatias lazy; SPECs da M17-F01 (#200), M17-F02 (#202) e M17-F03 `aprovada-pi` (2026-08-30), demais sem SPEC; docs em `docs/mvp/mvp-017…021` | 0/18 |
| MVP-022 Shell de produto | [#205](https://github.com/RodReis/rrb-jarvisOS/issues/205) | menu do JARVIS/Agents OS projetado do registro de módulos; **SPEC da F01 `aprovada-pi` (2026-08-30)**, oito decisões do PI; entra depois do MVP-026 | 0/1 |
| MVP-023 Biblioteca de Blueprints | [#212](https://github.com/RodReis/rrb-jarvisOS/issues/212) | quatro SPECs `aprovada-pi`; #214–#217 em Backlog | 0/4 |
| MVP-024 Gestão de Portfólio | [#213](https://github.com/RodReis/rrb-jarvisOS/issues/213) | quatro SPECs `aprovada-pi`; #218–#221 em Backlog | 0/4 |
| MVP-025 Jornada de planejamento por IA | [#237](https://github.com/RodReis/rrb-jarvisOS/issues/237) | corrige o fluxo inicial do MVP-008. **M25-F01 a F05 aceitas pelo PI em 2026-09-03** (`proplan:finalizado`); fechamento do épico é ato dele; doc em `docs/mvp/mvp-025-jornada-planejamento-ia.md` | 5/5 |
| MVP-026 Fases, modelo por fase e console da geração | [#250](https://github.com/RodReis/rrb-jarvisOS/issues/250) | criado em 2026-09-04; corrige a superfície do MVP-025 (fase no card, modelo por fase, console com ferramentas, marcos Git antes da Construção, Codex no ponto único). seis SPECs `aprovada-pi`; doc em `docs/mvp/mvp-026-fases-modelos-e-console.md`. **As sete fatias entregues** — F03, F04 e F05 já aceitas pelo PI. A M26-F06 (PR [#269](https://github.com/RodReis/rrb-jarvisOS/pull/269)) fechava o MVP; a **M26-F07** ([#274](https://github.com/RodReis/rrb-jarvisOS/issues/274), PR [#278](https://github.com/RodReis/rrb-jarvisOS/pull/278)) nasce depois dele, da **emenda E1** à SPEC-Fases-03 aprovada em 2026-09-05 — o teste do PI mostrou que o CLI gerava documento com a governança do repositório dentro. O fechamento do épico é ato do PI | 7/7 |

## Índice Fatia ↔ SPEC

Não existe catálogo global `SPEC-nnn`. O slug identifica a SPEC; os números abaixo não são reaproveitados. Uma SPEC `rascunho-completo` ainda não autoriza construção.

| Índice | MVP | Fatia | SPEC |
|---|---|---|---|
| F01–F06 | MVP-001 | Bootstrap · AppShell · Auth · Dados · Settings · Observabilidade | `spec-fundacao-01..06` |
| M2-F01–F05 | MVP-002 | Supabase · Policy · Allowlist · Workflows · Simulação | `spec-execucao-local-01..05` |
| M3-F01–F06 | MVP-003 | Infra · Foundations · Componentes a/b · AppShell a/b · Identidades · Hardening | `spec-design-system-01..06` |
| M4-F01 | MVP-004 | Filesystem allowlisted | `spec-execucao-real-01-filesystem-allowlisted.md` |
| M4-F02 | MVP-004 | Terminal controlado | `spec-execucao-real-02-terminal-controlado.md` |
| M4-F03 | MVP-004 | UI da allowlist de diretórios ([#110](https://github.com/RodReis/rrb-jarvisOS/issues/110)) | `spec-execucao-real-03-ui-allowlist-diretorios.md` |
| M5-F01 | MVP-005 | Vault | `spec-providers-01-vault-credenciais.md` |
| M5-F02 | MVP-005 | Adapter Claude | `spec-providers-02-adapter-claude-api.md` |
| M5-F03 | MVP-005 | BudgetPolicy | `spec-providers-03-budget-policy.md` |
| M5-F04 | MVP-005 | Multi-provider | `spec-providers-04-multi-provider-roteamento.md` |
| M6-F01 | MVP-006 | Núcleo de conectores | `spec-conectores-01-nucleo.md` |
| M6-F02 | MVP-006 | Operação e governança | `spec-conectores-02-operacao-governanca.md` |
| M6-F03 | MVP-006 | GitHub App/autenticação | `spec-conectores-03-github-app-autenticacao.md` |
| M6-F04 | MVP-006 | Automação GitHub | `spec-conectores-04-github-automacao.md` |
| M6-F05 | MVP-006 | Tavily Search | `spec-conectores-05-tavily-search.md` |
| M6-F06 | MVP-006 | Tavily Extract/evidências | `spec-conectores-06-tavily-extract-evidencias.md` |
| M7-F01 | MVP-007 | Núcleo, identidade e persistência ([#180](https://github.com/RodReis/rrb-jarvisOS/issues/180)) | `spec-memoria-01-nucleo-identidade-persistencia.md` — aprovada-pi |
| M7-F02 | MVP-007 | Fontes iniciais, ingestão e retomada ([#181](https://github.com/RodReis/rrb-jarvisOS/issues/181)) | `spec-memoria-02-fontes-ingestao-retomada.md` — aprovada-pi |
| M7-F03 | MVP-007 | Recuperação contextual e orçamento ([#182](https://github.com/RodReis/rrb-jarvisOS/issues/182)) | `spec-memoria-03-recuperacao-contextual-orcamento.md` — aprovada-pi |
| M7-F04 | MVP-007 | Retenção e reconstrução da memória ([#183](https://github.com/RodReis/rrb-jarvisOS/issues/183)) | `spec-memoria-04-retencao-reconstrucao.md` — aprovada-pi |
| M7-F05 | MVP-007 | Adapter opcional do Graphify ([#184](https://github.com/RodReis/rrb-jarvisOS/issues/184)) | `spec-memoria-05-adapter-graphify.md` — aprovada-pi |
| M7-F06 | MVP-007 | Memória operacional e validação de lições ([#185](https://github.com/RodReis/rrb-jarvisOS/issues/185)) | `spec-memoria-06-validacao-licoes-operacionais.md` — aprovada-pi |
| M7-F07 | MVP-007 | Agent Memory e Notebook ([#186](https://github.com/RodReis/rrb-jarvisOS/issues/186)) | a redigir |
| M7-F08 | MVP-007 | Resiliência e prova integrada ([#187](https://github.com/RodReis/rrb-jarvisOS/issues/187)) | a redigir |
| M8-F01 | MVP-008 | Projeto e Git local | `spec-planejamento-01-projeto-git-local.md` |
| M8-F02 | MVP-008 | Contexto, skills e orçamento | `spec-planejamento-02-contexto-skills-orcamento.md` |
| M8-F03 | MVP-008 | Wizard orientado | `spec-planejamento-03-wizard-orientado.md` |
| M8-F04 | MVP-008 | PRD/Landscape/Convention | `spec-planejamento-04-prd-landscape-convention.md` |
| M8-F05 | MVP-008 | Anexos/design/arquitetura | `spec-planejamento-05-anexos-design-arquitetura.md` |
| M8-F06 | MVP-008 | Roadmap e aprovações | `spec-planejamento-06-roadmap-aprovacoes.md` |
| M9-F01 | MVP-009 | Publicação GitHub | `spec-entrega-01-publicacao-github.md` |
| M9-F02 | MVP-009 | DAG/fila/reconciliação | `spec-entrega-02-dag-fila-reconciliacao.md` |
| M9-F03 | MVP-009 | Worktree/preflight/Docker | `spec-entrega-03-worktree-preflight-docker.md` |
| M9-F04 | MVP-009 | Construção/recuperação | `spec-entrega-04-construcao-recuperacao.md` |
| M9-F05 | MVP-009 | Revisão/CI/merge | `spec-entrega-05-revisao-ci-merge.md` |
| M9-F06 | MVP-009 | Evidência/limpeza | `spec-entrega-06-evidencia-limpeza-continuidade.md` |
| M10-F01 | MVP-010 | Runtime de executores ([#116](https://github.com/RodReis/rrb-jarvisOS/issues/116)) | `spec-multi-executor-01-runtime.md` |
| M10-F02 | MVP-010 | Autenticação Codex ([#117](https://github.com/RodReis/rrb-jarvisOS/issues/117)) | `spec-multi-executor-02-autenticacao-codex.md` — aprovada-pi; **emendada em 2026-09-04** (dependência da F01 só para o mount; sobe para antes da M26-F06) |
| M10-F03 | MVP-010 | Codex Exec Adapter ([#118](https://github.com/RodReis/rrb-jarvisOS/issues/118)) | `spec-multi-executor-03-codex-exec-adapter.md` |
| M10-F04 | MVP-010 | Roteamento e revisão cruzada ([#119](https://github.com/RodReis/rrb-jarvisOS/issues/119)) | `spec-multi-executor-04-roteamento-revisao-cruzada.md` |
| M10-F05 | MVP-010 | UI e prova operacional ([#120](https://github.com/RodReis/rrb-jarvisOS/issues/120)) | `spec-multi-executor-05-ui-prova-operacional.md` |
| M11-F01 | MVP-011 | Capacidades e perfis ([#122](https://github.com/RodReis/rrb-jarvisOS/issues/122)) | `spec-squads-01-capacidades-perfis.md` |
| M11-F02 | MVP-011 | Planejador e validador ([#123](https://github.com/RodReis/rrb-jarvisOS/issues/123)) | `spec-squads-02-planejador-validador.md` |
| M11-F03 | MVP-011 | Workers isolados ([#124](https://github.com/RodReis/rrb-jarvisOS/issues/124)) | `spec-squads-03-workers-isolados.md` |
| M11-F04 | MVP-011 | Revisão independente ([#125](https://github.com/RodReis/rrb-jarvisOS/issues/125)) | `spec-squads-04-revisao-independente.md` |
| M11-F05 | MVP-011 | Orçamento, cancelamento e E2E ([#126](https://github.com/RodReis/rrb-jarvisOS/issues/126)) | `spec-squads-05-orcamento-cancelamento-e2e.md` |
| M12-F01 | MVP-012 | Pool global e fila ([#128](https://github.com/RodReis/rrb-jarvisOS/issues/128)) | `spec-scheduler-01-pool-fila.md` |
| M12-F02 | MVP-012 | Independência e locks ([#129](https://github.com/RodReis/rrb-jarvisOS/issues/129)) | `spec-scheduler-02-independencia-locks.md` |
| M12-F03 | MVP-012 | Isolamento concorrente ([#130](https://github.com/RodReis/rrb-jarvisOS/issues/130)) | `spec-scheduler-03-isolamento-concorrente.md` |
| M12-F04 | MVP-012 | Merge serializado ([#131](https://github.com/RodReis/rrb-jarvisOS/issues/131)) | `spec-scheduler-04-merge-serializado.md` |
| M12-F05 | MVP-012 | Recuperação e E2E ([#132](https://github.com/RodReis/rrb-jarvisOS/issues/132)) | `spec-scheduler-05-recuperacao-e2e.md` |
| M13-F01 | MVP-013 | Inventário e DAG ([#134](https://github.com/RodReis/rrb-jarvisOS/issues/134)) | `spec-continuo-01-inventario-dag.md` |
| M13-F02 | MVP-013 | Dispatcher e retomada ([#135](https://github.com/RodReis/rrb-jarvisOS/issues/135)) | `spec-continuo-02-dispatcher-retomada.md` |
| M13-F03 | MVP-013 | Controles operacionais ([#136](https://github.com/RodReis/rrb-jarvisOS/issues/136)) | `spec-continuo-03-controles-operacionais.md` |
| M13-F04 | MVP-013 | Projeções e gates ([#137](https://github.com/RodReis/rrb-jarvisOS/issues/137)) | `spec-continuo-04-projecoes-gates.md` |
| M13-F05 | MVP-013 | Jornada multi-MVP ([#138](https://github.com/RodReis/rrb-jarvisOS/issues/138)) | `spec-continuo-05-jornada-multi-mvp.md` |
| M14-F01 | MVP-014 | Núcleo de release e fila ([#150](https://github.com/RodReis/rrb-jarvisOS/issues/150)) | `spec-release-01-nucleo-estados-fila.md` |
| M14-F02 | MVP-014 | Docker, artefatos e configuração ([#151](https://github.com/RodReis/rrb-jarvisOS/issues/151)) | `spec-release-02-compose-artefatos-config.md` |
| M14-F03 | MVP-014 | Preview isolado por PR ([#152](https://github.com/RodReis/rrb-jarvisOS/issues/152)) | `spec-release-03-preview-isolado.md` |
| M14-F04 | MVP-014 | Staging e Produção automática ([#153](https://github.com/RodReis/rrb-jarvisOS/issues/153)) | `spec-release-04-staging-producao.md` |
| M14-F05 | MVP-014 | Compensação, retorno à V2 e E2E ([#154](https://github.com/RodReis/rrb-jarvisOS/issues/154)) | `spec-release-05-recuperacao-e2e.md` |
| M15-F01 | MVP-015 | Núcleo de eventos, outbox e projeções ([#156](https://github.com/RodReis/rrb-jarvisOS/issues/156)) | `spec-observabilidade-01-eventos-outbox-projecoes.md` |
| M15-F02 | MVP-015 | Reconciliação, saúde, custos e quotas ([#157](https://github.com/RodReis/rrb-jarvisOS/issues/157)) | `spec-observabilidade-02-reconciliacao-saude-custos-quotas.md` |
| M15-F03 | MVP-015 | Alertas e notificações ([#158](https://github.com/RodReis/rrb-jarvisOS/issues/158)) | `spec-observabilidade-03-alertas-notificacoes.md` |
| M15-F04 | MVP-015 | Consultas, retenção, rollups e CLI ([#159](https://github.com/RodReis/rrb-jarvisOS/issues/159)) | `spec-observabilidade-04-consultas-retencao-cli.md` |
| M15-F05 | MVP-015 | Console operacional ([#160](https://github.com/RodReis/rrb-jarvisOS/issues/160)) | `spec-observabilidade-05-console-operacional.md` |
| M15-F06 | MVP-015 | Resiliência, desempenho e prova E2E ([#161](https://github.com/RodReis/rrb-jarvisOS/issues/161)) | `spec-observabilidade-06-resiliencia-desempenho-e2e.md` |
| M16-F01 | MVP-016 | Fundação e ingestão ([#163](https://github.com/RodReis/rrb-jarvisOS/issues/163)) | `spec-aprendizado-01-fundacao-ingestao.md` |
| M16-F02 | MVP-016 | Memória de falhas ([#164](https://github.com/RodReis/rrb-jarvisOS/issues/164)) | `spec-aprendizado-02-memoria-falhas.md` |
| M16-F03 | MVP-016 | Registro e resolução de políticas ([#165](https://github.com/RodReis/rrb-jarvisOS/issues/165)) | `spec-aprendizado-03-registro-resolucao-politicas.md` |
| M16-F04 | MVP-016 | Experimentos e promoção ([#166](https://github.com/RodReis/rrb-jarvisOS/issues/166)) | `spec-aprendizado-04-experimentos-promocao.md` |
| M16-F05 | MVP-016 | Estratégias e recomendações assistidas ([#167](https://github.com/RodReis/rrb-jarvisOS/issues/167)) | `spec-aprendizado-05-estrategias-recomendacoes.md` — aprovada-pi |
| M16-F06 | MVP-016 | Interface, resiliência e prova E2E ([#168](https://github.com/RodReis/rrb-jarvisOS/issues/168)) | `spec-aprendizado-06-interface-resiliencia-e2e.md` — aprovada-pi; gate visual antes da construção |
| M17-F01 | MVP-017 | STT local + push-to-talk | `spec-voz-01-stt-local-push-to-talk.md` |
| M17-F02 | MVP-017 | TTS Piper + timeline de visemes | `spec-voz-02-tts-piper-fonemas.md` |
| M17-F03 | MVP-017 | Persona JARVIS no ponto único | `spec-voz-03-persona-ponto-unico.md` |
| M22-F01 | MVP-022 | Menu JARVIS OS / Agents OS | `spec-shell-01-menu-jarvis-agents-os.md` |

| M23-F01 | MVP-023 | Catálogo e revisões de Blueprints ([#214](https://github.com/RodReis/rrb-jarvisOS/issues/214)) | `spec-blueprints-01-catalogo-revisoes.md` — aprovada-pi |
| M23-F02 | MVP-023 | Instanciação e wizard orientado ([#215](https://github.com/RodReis/rrb-jarvisOS/issues/215)) | `spec-blueprints-02-instanciacao-wizard.md` — aprovada-pi |
| M23-F03 | MVP-023 | Anexos, compatibilidade e evolução ([#216](https://github.com/RodReis/rrb-jarvisOS/issues/216)) | `spec-blueprints-03-anexos-compatibilidade.md` — aprovada-pi |
| M23-F04 | MVP-023 | Interface e prova integrada de Blueprints ([#217](https://github.com/RodReis/rrb-jarvisOS/issues/217)) | `spec-blueprints-04-interface-resiliencia-e2e.md` — aprovada-pi; gate visual antes da construção |
| M24-F01 | MVP-024 | Catálogo e prontidão do portfólio ([#218](https://github.com/RodReis/rrb-jarvisOS/issues/218)) | `spec-portfolio-01-catalogo-prontidao.md` — aprovada-pi |
| M24-F02 | MVP-024 | Prioridade e controles canônicos ([#219](https://github.com/RodReis/rrb-jarvisOS/issues/219)) | `spec-portfolio-02-prioridade-controles.md` — aprovada-pi |
| M24-F03 | MVP-024 | Custos, quotas e capacidade do portfólio ([#220](https://github.com/RodReis/rrb-jarvisOS/issues/220)) | `spec-portfolio-03-custos-quotas.md` — aprovada-pi |
| M24-F04 | MVP-024 | Console de portfólio e prova integrada ([#221](https://github.com/RodReis/rrb-jarvisOS/issues/221)) | `spec-portfolio-04-console-resiliencia-e2e.md` — aprovada-pi; gate visual antes da construção |
| M25-F01 | MVP-025 | Estado do projeto e jornada única ([#238](https://github.com/RodReis/rrb-jarvisOS/issues/238)) | `spec-jornada-01-estado-e-jornada-unica.md` — aprovada-pi; **entregue** (gate visual cumprido em 2026-09-03) |
| M25-F02 | MVP-025 | Prompt e brief refinado por IA ([#239](https://github.com/RodReis/rrb-jarvisOS/issues/239)) | `spec-jornada-02-prompt-e-brief-por-ia.md` — aprovada-pi; **entregue** (gate visual cumprido em 2026-09-03) |
| M25-F03 | MVP-025 | PRD, Landscape e Convention por IA ([#240](https://github.com/RodReis/rrb-jarvisOS/issues/240)) | `spec-jornada-03-prd-landscape-convention-por-ia.md` — aprovada-pi; **entregue** |
| M25-F04 | MVP-025 | Arquitetura por IA após os anexos ([#241](https://github.com/RodReis/rrb-jarvisOS/issues/241)) | `spec-jornada-04-arquitetura-por-ia.md` — aprovada-pi; **entregue** |
| M25-F05 | MVP-025 | Roadmap, MVPs e SPEC por IA ([#242](https://github.com/RodReis/rrb-jarvisOS/issues/242)) | `spec-jornada-05-roadmap-mvp-spec-por-ia.md` — aprovada-pi; **entregue** |
| M26-F01 | MVP-026 | Fase do projeto e card completo ([#251](https://github.com/RodReis/rrb-jarvisOS/issues/251)) | `spec-fases-01-fase-e-card-do-projeto.md` — aprovada-pi; **entregue** (PR #258); gate visual apresentado |
| M26-F02 | MVP-026 | Catálogo de modelos e modelo por fase ([#252](https://github.com/RodReis/rrb-jarvisOS/issues/252)) | `spec-fases-02-catalogo-e-modelo-por-fase.md` — aprovada-pi; **entregue**, aguardando aceite |
| M26-F03 | MVP-026 | Console da geração ([#253](https://github.com/RodReis/rrb-jarvisOS/issues/253)) | `spec-fases-03-console-da-geracao.md` — aprovada-pi; **entregue** (PR [#263](https://github.com/RodReis/rrb-jarvisOS/pull/263)) |
| M26-F04 | MVP-026 | Marcos Git visíveis e gate da construção ([#254](https://github.com/RodReis/rrb-jarvisOS/issues/254)) | `spec-fases-04-marcos-git-e-gate-da-construcao.md` — aprovada-pi; **entregue** (PR [#264](https://github.com/RodReis/rrb-jarvisOS/pull/264)). Coerência blob↔revisão por SHA-256 recalculado do blob, **não** por `git hash-object` (impossível: SHA-256 de texto vs SHA-1 de blob) — decisão do PI de 2026-09-04 |
| M26-F05 | MVP-026 | Modelo da fase Construção no run ([#255](https://github.com/RodReis/rrb-jarvisOS/issues/255)) | `spec-fases-05-modelo-da-fase-no-run.md` — aprovada-pi |
| M26-F06 | MVP-026 | Codex como provider do ponto único ([#256](https://github.com/RodReis/rrb-jarvisOS/issues/256)) | `spec-fases-06-codex-no-ponto-unico.md` — aprovada-pi; depende da M10-F02 |
| M26-F07 | MVP-026 | Isolamento do CLI por fase ([#274](https://github.com/RodReis/rrb-jarvisOS/issues/274)) | `spec-fases-03-console-da-geracao.md` § **Emenda E1** — aprovada-pi (2026-09-05); depende das correções [#271](https://github.com/RodReis/rrb-jarvisOS/issues/271) e [#272](https://github.com/RodReis/rrb-jarvisOS/issues/272) |
| — | MVP-025 | [FIX] A jornada não saía do prompt ([#281](https://github.com/RodReis/rrb-jarvisOS/issues/281)) e o schema do brief proibia as pendências ([#280](https://github.com/RodReis/rrb-jarvisOS/issues/280)) | `spec-jornada-02-prompt-e-brief-por-ia.md` § Etapa Prompt, § Refinamento e § Brief (critério 1) — correção de comportamento já definido, sem spec nova; a decisão do brief no fim do refinamento é do PI (2026-09-05) |

## Decisões do PI no MVP-026 (2026-09-04)

Levantadas depois de o PI usar o app com o MVP-025 aceito. Detalhe em `docs/mvp/mvp-026-fases-modelos-e-console.md`; emenda em `docs/DECISIONS.md`.

1. **Três fases derivadas da etapa** — Planejamento (`prompt`…`pacote-aceito`), Especificação (`roadmap`…`spec-aceita`), Construção (`construcao`). Descartado fase por gate.
2. **Modelo por fase: default no workspace + override por projeto**, auditados. Planejamento/Especificação = Fable 5.1 ou Sol (`gpt-5.6-sol`); Construção = Opus 5 ou `gpt-5.5`. **Fable 5.1 só pela rota de assinatura, nunca pela API.**
3. **Codex vira provider do ponto único** (assinatura do Codex) para Planejamento/Especificação; **M10-F02 sobe na fila**, imediatamente antes da M26-F06. Codex como executor da Construção continua M10-F03/F04.
4. **Modelo da fase Construção chega ao run do MVP-009** em fatia própria (M26-F05); sem modelo por passo do run.
5. **Card:** fase + etapa, gates aceitos (n/5) + data, rota + modelo, pendência/bloqueio.
6. **Console da geração persistido por geração**, ferramentas em nome + resumo + status, painel retrátil na própria etapa.
7. **Marcos Git:** painel na tela do projeto **e** bloqueio no `SLICE_ENTRY` (marco sem commit, blob divergente ou worktree sujo).
8. **Fila:** MVP-026 antes do MVP-022 e do Command Center; [#251](https://github.com/RodReis/rrb-jarvisOS/issues/251) recebe `proplan:next`.
9. "Roteamento por tipo de tarefa" (SPEC-Providers-04) **não é removido** — vira "Avançado"; a jornada não o consome, MVP-007/017/021 consumirão.
10. Ids confirmados: `claude-fable-5-1`; `qwen3:8b`; `gpt-5.6-sol`, `gpt-5.5`, `gpt-5.4` (Codex CLI 0.149.0).
11. **SPEC-Multi-Executor-02 emendada:** a dependência da M10-F01 vale só para o mount no container; a parte de host entra antes da M26-F06. Descartado puxar a M10-F01 e descartado a F06 criar o perfil sozinha.

## Decisões do PI no MVP-025 (2026-09-03)

Levantadas depois de o PI testar o app (`projeto1`) e constatar que o fluxo inicial não era o desenhado em 2026-08-28. Detalhe e causa em `docs/mvp/mvp-025-jornada-planejamento-ia.md`; emenda ao design em `docs/DECISIONS.md`.

1. **Toda geração do planejamento é por IA** (brief, PRD/Landscape/Convention, arquitetura, roadmap e SPEC), com origem por afirmação e `proposto` marcado e cortado item a item pelo PI. Reverte o "compor em vez de gerar" da M8-F04.
2. **Três aceites antes da construção:** `BRIEF_ACCEPTED`, `PRD_ACCEPTED` e `PROJECT_PACKAGE`. Landscape e Convention saem com o PRD; arquitetura só depois dos anexos (decisão 9 do design mantida).
3. **Prompt é a primeira etapa após criar o projeto**; projeto sem brief (inclusive `projeto1`) volta para ela sem perder decisões gravadas.
4. **Rota de assinatura (Claude MAX via Claude Code CLI) é a rota da geração**, sem valor monetário no ledger; rota paga só por opt-in explícito, nunca fallback.
5. **Fila:** logo após o MVP-009 fechar, antes do MVP-022 e do Command Center. **As cinco fatias foram entregues e aceitas em 2026-09-03.**
6. Projeto aberto tem **rota própria**; blocos 1 e 9 do `ProjectBriefSchema` pré-preenchidos; PRD aceitável sem pesquisa de mercado (Landscape pendente e visível); termo de pesquisa proposto pela IA e confirmado pelo PI; `DECISIONS.md` gerado registra decisões do PI e propostas da IA como ADRs; no `MVP_ENTRY` o PI escolhe entre os MVPs elegíveis.

## Decisões do PI na M9-F06 (2026-09-02)

Dois pontos abertos foram levantados **antes** de codificar e decididos pelo PI. Os dois mudavam o trabalho, então nenhum foi assumido.

1. **O container do executor passa a nascer com `--rm`.** O critério 5 da SPEC-Entrega-06 exige remover o container, mas `docker rm|rmi|prune|down` casa a política de destrutivos do MVP-004 e abriria `ApprovalRequest` — travando a limpeza automática num gate humano, que é o oposto do desenvolvimento autônomo que esta fatia entrega. Com a flag na criação, `docker stop` já remove, sem tocar a allowlist. As alternativas oferecidas eram liberar `docker rm` (rejeitada: reintroduz o gate humano) e apenas parar, deixando pendência (rejeitada: containers parados acumulariam até alguém limpar à mão). Antes de aplicar, foi verificado que nada na M9-F03 lê logs do container depois que ele para.

2. **A jornada E2E real completa não roda nesta entrega.** A spec a pede em projeto e repositório exclusivos e descartáveis, com orçamento limitado e fora da suíte padrão — o que exige autorização e um repositório do PI. O limite fica **declarado** no `docs/DEVELOPMENT.md`, na mesma postura que a M9-F01 adotou para o smoke real do push autenticado. Marcar como feito o que não rodou seria o fechamento frágil que o processo existe para impedir.

## Decisões do PI na M9-F05 (2026-09-02)

Três pontos abertos foram levantados durante a construção e **decididos pelo PI**, que delegou a escolha ao Code pedindo "o melhor para o desenvolvimento autônomo com agents". Registradas aqui porque mudam comportamento e não podem virar escolha silenciosa de implementação.

1. **`verificarEscopo` passa a falhar fechado, com causa `externo`.** O fail-open (`if (!status.ok) return { ok: true }`) ficou pendente na M9-F04 e colide com o critério 1 desta fatia — com o `git status` do container falhando, o escopo não é verificado e o push sai. **Não é escopo novo:** a `ARCHITECTURE.md` § Segurança já decide *"fail closed: ação não reconhecida pela política é bloqueada, não permitida"*, e um `git status` que não responde é exatamente isso. A causa é `externo`, não `risco-usuario`: a distinção governa a retomada — `risco-usuario` diz que o agente escreveu fora do escopo, `externo` diz que a ferramenta falhou, e rotular errado mandaria procurar um arquivo indevido que não existe. Em desenvolvimento autônomo, fail-open significa um container degradado publicando sem ninguém ter verificado o escopo.

2. **Teto de espera do CI: configurável por projeto, padrão 30 minutos, terminando em `AWAITING_MERGE`.** A SPEC não fixa quanto o gate aguarda checks pendentes. Estourar o teto não é falha do código — é CI mais lenta que o esperado —, e `BLOCKED` ensinaria a ler bloqueio como ruído, pela mesma razão que a M9-F02 recusou `BLOCKED` para o kill-switch desligado. O PR fica verde ou pendente e o run termina explicável, o que mantém a fila girando: com WIP=1 global, um run preso esperando CI eterna trava todos os outros.

3. **O check obrigatório é `validacao`, e a pipeline o exige na proteção no mesmo run em que gera o workflow.** Correção ao plano original, que previa terminar em bloqueio quando a proteção não exigisse o nome gerado: a M9-F01 já escreve a proteção via `branch.ensure-protection`, que aceita `checksExigidos`. Então a pipeline não inventa nome nem depende de alguém ter configurado à mão — ela declara `validacao` como obrigatório junto com o workflow, fechando os critérios 9 e 10 sem intervenção humana, que é o requisito do desenvolvimento autônomo. Contexts que o projeto já exija **somam**, não são substituídos, e o gate espera por todos.

## Próximas ações

1. **[#251](https://github.com/RodReis/rrb-jarvisOS/issues/251) (M26-F01), [#252](https://github.com/RodReis/rrb-jarvisOS/issues/252) (M26-F02) e [#253](https://github.com/RodReis/rrb-jarvisOS/issues/253) (M26-F03) entregues** — fechamento e `proplan:finalizado` são atos do PI. A cabeça da fila é [#254](https://github.com/RodReis/rrb-jarvisOS/issues/254) (M26-F04, `proplan:next`). Ordem do MVP-026: F01 → F02 → F03 → F04 → F05 → M10-F02 ([#117](https://github.com/RodReis/rrb-jarvisOS/issues/117)) → F06 ([#256](https://github.com/RodReis/rrb-jarvisOS/issues/256)). Todas em Backlog. MVP-025: filhas `proplan:finalizado`; fechamento do épico #237 é ato do PI. MVP-022 e Command Center vêm depois do MVP-026.
2. **Risco registrado:** a SPEC-Blueprints-02 (M23-F02) referencia o wizard da M8-F03, que a M25-F02 substitui por perguntas geradas — emenda a fazer quando o MVP-023 entrar na fila.
3. **#106 (M9-F06) entregue por PR — a última fatia do MVP-009.** Aceite formal do PI pendente na issue. **#105 (M9-F05) foi aceita em 2026-09-02** e entregue no PR #235. Os limites declarados da F06 estão no `docs/DEVELOPMENT.md` § Fatia 06 — o principal é a jornada E2E real completa, que **não rodou** por decisão do PI e continua exigindo repositório descartável e orçamento autorizado.
4. Conservar #167/#168 e #214–#221 em Backlog após o aceite exato de 2026-08-31, sem promover qualquer uma a `next` por esta atualização. #232 (`[INFRA][FIX]` do pool de testes) também nasce em Backlog, sem entrar na fila por conta própria.
5. Antes das fatias visuais, anexar `DESIGN-SYSTEM.md` e HTML pelo fluxo já aprovado; texto de SPEC/template não substitui anexo do PI.
6. M7-F05/F06 estão aprovadas/Backlog e M7-F07–F08 continuam a redigir; as fatias do Command Center seguem seus planejamentos próprios. Nenhuma delas bloqueia concluir a documentação V3.
7. O relatório de fechamento do PR #189 documenta reconciliação, ordem técnica, issues e validação. Aprovações prévias são preservadas, não repetidas.

## Roadmap

Base entregue → MVP-009 (finalizado) → MVP-025 (aceito) → **MVP-026 Fases, modelo por fase e console** (com a M10-F02 antes da M26-F06) → Shell MVP-022 → Command Center MVP-017 → MVP-018/MVP-019 → MVP-020 → MVP-021 (decisão do PI de 2026-09-04: o MVP-026 passa na frente do Shell e do Command Center). MVP-010–016 seguem a prioridade já definida pelo PI, sem ultrapassar essa sequência por causa deste PR.

V3: MVP-014 → MVP-015 → MVP-016; MVP-023 Blueprints depende de MVP-008; MVP-024 Portfólio depende de MVP-012/MVP-015. Ordem de apresentação 014, 015, 016, 023, 024 não cria dependência técnica entre Blueprints e Portfólio. MVP-007 permanece paralelo/não bloqueante.
