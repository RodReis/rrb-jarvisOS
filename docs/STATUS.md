# STATUS.md — Kanban / Roadmap

Atualizado em: **2026-08-30**. Visão curta do estado corrente e fonte única do índice Fatia ↔ SPEC. Histórico e ressalvas: `docs/STATUS-ARQUIVO.md`. O board GitHub vence em caso de divergência factual.

## Agora

| Coluna | Item | Estado |
|---|---|---|
| Em Andamento | [#101](https://github.com/RodReis/rrb-jarvisOS/issues/101) · MVP-009 F01 Publicação automática no GitHub | `proplan:doing` (2026-08-30); branch `feat/publicacao-github`. **Ler § Emendas da SPEC-Entrega-01 antes de fechar a entrega** |
| Próximo | [#102](https://github.com/RodReis/rrb-jarvisOS/issues/102) · MVP-009 F02 DAG, fila WIP=1 global e reconciliação | cabeça da fila; `proplan:next` aplicado. SPEC emendada em 2026-08-30 (WIP global, `AWAITING_MERGE`, `AWAITING_PI` = `SLICE_ENTRY`) |
| Backlog | [#103–#106](https://github.com/RodReis/rrb-jarvisOS/issues/103) · MVP-009 F03–F06 | SPECs `aprovada-pi` (2026-08-29) com **emendas do PI em 2026-08-30** (proxy no host, CI gerado, validação no container, docs no PR) |
| Feito | [#99](https://github.com/RodReis/rrb-jarvisOS/issues/99) · MVP-008 F06 Roadmap e aprovações | entregue PR [#178](https://github.com/RodReis/rrb-jarvisOS/pull/178), aguardando aceite — **fecha o MVP-008** |
| Finalizado | #87–#92 · MVP-006 F01–F06 | aceitos pelo PI (2026-08-30); épico [#86](https://github.com/RodReis/rrb-jarvisOS/issues/86) aguarda fechamento |
| Finalizado | #94–#98 · MVP-008 F01–F05 | aceitos pelo PI (2026-08-30) |
| Finalizado | [#170](https://github.com/RodReis/rrb-jarvisOS/issues/170) · `[FIX]` erro de git sem saída em Projetos | fechada pelo PI (2026-08-30) com `proplan:done` — falta trocar para `proplan:finalizado` no board |
| Finalizado | [#172](https://github.com/RodReis/rrb-jarvisOS/issues/172) · `[FIX]` Configurações em abas | aceito pelo PI (2026-08-30), PR [#173](https://github.com/RodReis/rrb-jarvisOS/pull/173) |
| Planejado | [#180–#187](https://github.com/RodReis/rrb-jarvisOS/issues/180) · MVP-007 F01–F08; [#167–#168](https://github.com/RodReis/rrb-jarvisOS/issues/167) · MVP-016 F05–F06 | `proplan:planejado`; SPECs em `rascunho` na branch `codex/pipeline-v2-design` (PR de docs pendente) — **fora da fila** |
| Backlog (fora da fila) | #116–#166 · MVP-010 a MVP-016 | `proplan:backlog`; SPECs marcadas `aprovada-pi` **só existem na branch `codex/pipeline-v2-design`** — não entram na fila até o PR de docs chegar à `main` |
| Épicos (fora da fila) | [#193–#197](https://github.com/RodReis/rrb-jarvisOS/issues/193) · MVP-017 a MVP-021 (Command Center) | `proplan:mvp` criados em 2026-08-30 com checklist de fatias (nascimento lazy — sem SPEC); entram na fila **depois do MVP-009**, antes dos MVP-010–016 (decisão do PI, 2026-08-30) |

> **M8-F06 entregue (2026-08-30) — o MVP-008 fecha.** Roadmap, DAG e gates por revisão. **A decisão que define a fatia é a separação entre gerar e aprovar**: se gerar promovesse o MVP para a fila, a geração estaria aprovando o que ela mesma propôs (critério 3); se aprovar aceitasse autor qualquer, a delegação aprovaria gate (critério 7). Os dois erros têm a mesma forma — *quem propõe decidindo que a proposta vale* —, e a fronteira entre os métodos é o que os impede; a tela repete a separação com dois botões, nunca um "gerar e aprovar". **O critério 7 é garantido pelo tipo, não por checagem:** `Approval.autor` é o literal `'pi'` e a tabela `approval` **não tem coluna `autor`** — ampliar o tipo faz o typecheck reprovar, verificado por contrafactual. **A estratégia do roadmap sai da decisão do PI**, não de heurística nossa: as duas opções de `escopo` descrevem roadmaps diferentes, e a composição só as escreve — `fatia-vertical` dá um MVP por jornada; `fundacao-ampla` põe um MVP de fundação antes, e o DAG reflete isso literalmente. Uma terceira estratégia exigiria uma terceira opção no wizard. **Sem decisão de escopo, ou sem jornada prototipada, o roadmap sai vazio** — assumir uma estratégia seria escolher pelo PI, e inventar jornada seria prometer o que ninguém desenhou. **Sem sessão autenticada não há aprovação:** `identidade` é `auth.usuarioAtual()?.id`, e **não** `userIdAtual` — este cai no usuário local, que existe sempre e faria toda aprovação passar como se houvesse alguém logado; "o usuário local" não é ninguém, e o critério 4 pergunta *quem* aceitou. **O critério 5 compara o conjunto de hashes**, não o instante nem a lista ordenada: a ordem de registro não é fato sobre a revisão, e ordená-la faria um pacote idêntico parecer novo. **Mudança cosmética não invalida nada** (invariante 4) — se corrigir um typo derrubasse o gate, o PI reaprovaria por ruído até parar de ler o que aprova. **A SPEC gerada nasce `rascunho`, com pergunta em aberto:** nascer aprovada faria a geração aprovar a si mesma, e nascer sem pergunta sugeriria que não há nada a decidir. **1822 testes** (+95) e **53 E2E** (+2), número da execução em série: 103/103 arquivos. **O E2E prova o que só o app real prova: sem sessão, não aprova** — o gate *tem* o que aprovar (as revisões existem) e mesmo assim recusa, sem gravar linha; no int-spec a identidade é injetada, então "sem sessão" seria condição simulada. **Contrafactuais medidos:** remover a exigência de identidade reprova 2 testes, fazer a geração promover MVP reprova 3, remover a guarda do DAG reprova 1. **Duas coisas que o teste pegou:** auto-dependência era reportada duas vezes (como `auto-dependencia` e de novo como `ciclo`), o mesmo fato com dois nomes; e o dublê da tela devolvia as mesmas revisões para os três gates, o que teria feito o teste medir o dublê em vez do produto. **Uma costura declarada:** o compositor virou injetável porque o de produção nunca gera ciclo, e a guarda do critério 1 seria inalcançável em teste — ela não é código morto (protege contra o compositor mudar e contra roadmap corrompido no banco), faltava o caminho para provocá-la. **Limites:** o caminho feliz de aprovação não é exercitável no E2E (sem consentimento do Google não há sessão, por desenho — ele vive no int-spec); uma fatia por MVP na composição, porque fatiar mais fino exigiria saber o que a jornada tem dentro; o `STATUS-ARQUIVO.md` nasce vazio, porque projeto recém-planejado não tem histórico e preenchê-lo seria inventar passado; `MVP_ENTRY` promove o primeiro da ordem topológica, não um escolhido pelo PI, porque a spec não define essa escolha; nenhuma chamada de IA; e a verificação visual do painel é do PI.

> **M8-F05 entregue (2026-08-30).** Anexos de design, validação de protótipos e o pacote de arquitetura — e a decisão que define a fatia é **o gate contar o ato, não a presença do arquivo**. O anexo entra por seletor que **copia para o projeto e hasheia no instante** (decisão do PI, 2026-08-29), gerando `AuditEvent`; `Anexo` não tem forma de existir sem `hash` e `anexadoEm`, que é o critério 7 virando tipo. **Não existe varredura de diretório, de propósito:** arquivo largado na pasta não tem instante definido, e é justamente o instante que este gate precisa ter preciso — o E2E prova isso com os dois arquivos no disco, nos caminhos exatos, e o gate barrando mesmo assim. **Não existe origem "gerado" nos anexos**, pelo mesmo motivo que não existe origem "modelo" nas afirmações: uma variante dizendo *"o modelo produziu este DESIGN-SYSTEM"* faria o gate se satisfazer sozinho, e o ato do PI — a coisa inteira que ele mede — viraria opcional. **O critério 3 virou coluna, não convenção:** `pacote_estrutural_id` amarra a revisão do PRD que a arquitetura assume, porque sem o ponteiro *"a mesma revisão"* seria *"o PRD mais recente"*, que muda sob os pés dela. **Os anexos entram no hash da arquitetura** (critério 6) — dois pacotes sobre protótipos diferentes podem produzir o mesmo texto e colidiriam no UNIQUE, apagando a diferença que os anexos fazem. **A validação usa o Chromium que o app já tem** (decisão do PI, 2026-08-30): `BrowserWindow` oculto, **mais restrito que o renderer, não igual** — sem preload, rede negada, partição efêmera, timeout duro; trazer o Playwright ao runtime empacotaria um segundo navegador para observar o que o primeiro já observa. **1727 testes** (+72) e **51 E2E** (+3), número carimbado da execução em série (`--no-file-parallelism`): 98/98 arquivos, zero erro. **O E2E achou três defeitos que os dublês escondiam:** `telasDoPrd` comparava a seção Escopo do PRD com headings de protótipo, mas o Escopo carrega **decisões**, não nomes de tela — achado falso garantido em todo projeto, e o int-spec não via porque fabricava o PRD com `'Tela de login'`; os dois marcos emitidos em sequência deixavam `arquitetura-aprovada` **sem hash de commit**, porque o primeiro levava tudo e o segundo não tinha o que commitar (`design-anexado` passou para o ato de anexar); e a evidência do achado vinha **duplicada** (o Chromium entrega o mesmo evento pelo caminho legado e pelo novo) e **suja** com o aviso de CSP do próprio Electron, que descrevia o protótipo do PI como problemático por uma decisão nossa. **Contrafactual medido:** remover o gate deixa 3 vermelhos — e um contrafactual anterior **passou**, revelando que o teste do "arquivo largado" media `pendencias()` em vez de `gerarArquitetura()`: garantia só vale onde a decisão acontece. **Limites:** a comparação "tela do PRD sem protótipo" não existe hoje e a lista vazia é a resposta correta (o critério 4 segue garantido na origem, porque os fluxos só saem das jornadas do protótipo); o `ARCHITECTURE.md` descreve fluxos e decisões, não desenho técnico, porque diagrama exigiria conhecimento que nenhuma decisão registrada contém; nenhuma chamada de IA nesta fatia; a contagem da suíte oscila em paralelo (1715–1727) sem jamais marcar teste como falho — mesmo evento de worker do Vitest da M8-F03, e por isso o número carimbado é o da execução em série; e a verificação visual do painel é do PI.

> **M8-F04 entregue (2026-08-30).** PRD, Landscape e Convention rastreáveis — e a decisão que define a fatia é **compor em vez de gerar**. Uma `AfirmacaoDoPacote` **não existe sem `origem`**, e as origens são exatamente duas: `decisao` (o PI respondeu no wizard) e `evidencia` (foi extraído de fonte externa, com URL e hash). **Não existe origem "modelo"** — e é isso que torna os critérios 1 e 5 estruturais em vez de prometidos: com prosa gerada, *"não contém regra inventada"* dependeria de auditar texto, e o verificador da M6-F06 declara o próprio limite (ele confere que a afirmação *cita* fonte extraída, não que a fonte a sustenta semanticamente). Sobre esse limite não se constrói garantia de não-invenção; sobre composição, sim. **O não-objetivo sai da opção preterida**, que é o critério 4 literal: as opções da pergunta são mutuamente exclusivas, então escolher uma é, no mesmo ato, recusar a outra — e as duas afirmações carregam a mesma origem, porque são a mesma decisão. **A pesquisa passa pelo `ConnectorService`, nunca pelo `TavilyAdapter`**: o gate de créditos vive dentro do `call()`, e um adapter injetado seria o segundo caminho sem gate. **Search descobre, Extract confirma** — o snippet da busca não vira afirmação, e nenhuma fonte entra no documento sem extração. **O bloqueio compõe os cinco campos da CONVENTION §4 porque o `ConnectorError` só tem três**: falta-lhe *tentativas* e *por que continuar seria incorreto*, e é esse último que distingue bloqueio de erro qualquer. **Context7 ficou fora, como a spec decide:** o PI pediu Context7 para a pesquisa de concorrentes durante a implementação; levantei que a spec § Perguntas resolvidas e a decisão 5 do MVP-008 cravam o contrário — e que o Context7 serve documentação de biblioteca, não cenário competitivo — e **a decisão registrada foi mantida**. **1655 testes** (+87) e **48 E2E** (+2). **O E2E prova no app real o que nenhum teste de componente alcança:** os três arquivos existem no disco com a marca de origem, a Convention não importou `proplan:` deste repositório, o marco `prd-aprovado` virou commit, regerar o mesmo conteúdo é a **mesma revisão**, e **sem credencial da Tavily a pesquisa bloqueia sem escrever nada**. **Contrafactual medido:** remover o gate de bloqueio deixa 5 testes vermelhos. **Três coisas que só o teste pegou:** uma regressão introduzida nesta própria fatia (o painel do pacote é filho do de contexto, e dois testes quebraram por falta de `listarPacotes` no dublê); o backtick dentro do template literal da migration, que abortou o parse do esbuild; e a guarda de schema antigo cobrando as tabelas novas. **Limites:** a Tavily real não é exercitada no E2E (sem credencial o conector recusa antes da rede, que é o caminho do critério 3; a API tem smoke próprio na M6-F05/F06); nenhuma chamada de IA nesta fatia; o Landscape registra o que as fontes disseram, **sem síntese nossa**, porque síntese seria conclusão sem evidência; `ArtifactRevision` não foi criado por não ter consumidor; um evento isolado de worker do Vitest não se reproduziu em cinco execuções e ficou registrado como infraestrutura, não como número carimbado sob suspeita; e a verificação visual do painel é do PI.

> **M8-F03 entregue (2026-08-30).** Uma decisão de cada vez, com a trilha de quem decidiu. **O backend do wizard já existia e não tinha consumidor** — `abrirSessao`/`salvarRespostas`, canais e preload estão prontos desde a M8-F01, e o campo `etapa` era declarado *"opaco para esta fatia"*: o slot deixado à espera desta. **A decisão estrutural foi dar tabela própria à `Decision`** (migration 18) em vez de mais um campo no JSON de `respostas`: o JSON é o rascunho e **sobrescreve**, e o que ele não guarda é justamente o que os critérios 3 e 5 exigem — **autoria por decisão** e a **decisão anterior**. Sem as duas, a trilha não distinguiria a escolha do PI da escolha delegada ao agente (invariante 3 do CONVENTION §4), e a contradição não teria o que mostrar antes de substituir. `decision` é **append-only por desenho, não por convenção**: o repositório não tem `update` nem `delete`, e revisar uma resposta insere outra linha com `substituiu` apontando para a anterior — um método de update deixaria o critério 5 dependendo de ninguém chamá-lo. **A contradição é devolvida, nunca aplicada:** o serviço só grava com `aceitarSubstituicao` explícito, e a tela reenvia *a mesma* resposta que a gerou — remontá-la arriscaria mandar algo diferente do que o PI leu. **A delegabilidade é recusada no domínio, não escondida na tela**, e o E2E prova isso chamando a ponte direto: `design-de-origem` volta `nao-delegavel` mesmo sem botão nenhum envolvido. **`autor` desconhecido vindo do banco vira `agente`, nunca `pi`** — linha corrompida não pode ganhar por acidente o único autor que aprova gate; o default fecha para o lado que não autoriza. **E a invariante 9 virou guarda estrutural:** `wizard-catalogo.spec.ts` varre o texto de todas as opções atrás de LGPD, consentimento, aceite duplo e classificação por domínio — enquanto a proibição vivesse só na cabeça de quem escreve a pergunta, a primeira que oferecesse "aceite dos termos" entraria sem nada falhar. **1568 testes** (+98) e **46 E2E** (+1). **Três coisas que só o teste pegou:** as duas guardas de enumeração da ponte acusaram os métodos novos (como nas fatias anteriores); o teste de migração de schema antigo quebrou com `table decision already exists`, cobrando que quem adiciona migration declare o que ela cria; e o lint de React barrou dois `setState` síncronos em efeito — o reset da escolha virou `key` no subcomponente (o reset idiomático) e o `carregando` booleano virou estado tri-valorado, eliminando o `setCarregando(false)` do `finally`. **O relatório também cobrou o próprio número:** uma execução gravou 477 de 486 no Banco com 9 `pending` que **não se reproduziram** — regerado, dá 486/486, que é a régua do TESTING §5 valendo (número só cola se sobrevive a reexecução independente). **Limites:** o catálogo cobre a etapa de contexto, não o wizard inteiro (PRD, anexos e roadmap trazem as suas em M8-F04/05/06); a recomendação é **dado, não geração** — nenhuma chamada de IA nesta fatia; `concluirMarco` não é chamado pelo wizard, porque fechar o contexto em commit é gate e gate é da M8-F06; e sem consentimento do Google o AppShell não monta, então a verificação visual do pop-up é do PI.

> **M8-F02 entregue (2026-08-30).** Nenhuma geração sem manifesto e sem orçamento — e o gate é **assinatura, não convenção**: `contextPackId` é campo do `AiRequest`, e o `AiCallService` recebe um `VerificadorDeContexto` no construtor, como já recebia o `BudgetService`. Um serviço que apenas *chamasse* o ponto único deixaria o critério 1 dependendo de todo chamador futuro lembrar. **1470 testes** (+112) e **45 E2E** (+4). **A rota de assinatura deixou de ser isenta por acidente:** ela passava pelo gate porque seu preço na tabela é zero, não porque a regra dissesse isso — o gate agora pergunta `isRotaUnmetered(provider)`, e no dia em que alguém "corrigisse" a tabela do `claude-code` para um número qualquer a rota do PI passaria a ser barrada sem decisão de ninguém. Ela registra uso **sem valor monetário**: `estimado_usd` fica `NULL`, não zero — zero afirmaria "custou nada", `NULL` diz "não se converte em USD". **O `unmetered = 0` no `totals` é filtro explícito**, não efeito colateral: a linha já tem `real_usd` NULL e o `SUM` a ignoraria, mas bastaria alguém preencher o campo "para não deixar coluna vazia" e o uso do MAX passaria a barrar a rota paga. **A migration 17 recria o `cost_event`** porque a v10 criou `estimado_usd` como `NOT NULL` — e a alternativa (gravar zero na assinatura) é exatamente a mentira que o critério 1a existe para impedir. **Segredo recusa o pack inteiro, não o item:** montar sem os arquivos acusados entregaria um contexto silenciosamente diferente do pedido, e o achado **nunca** carrega o trecho casado. **E o E2E achou o que nenhum teste unitário acharia: o handler do IPC descartava o `contextPackId`.** O `aiCall` reconstrói o `AiRequest` campo a campo, e o campo novo sumia no caminho — o gate estava certo, o transporte é que perdia o dado, e **toda chamada vinda da UI era recusada por falta de contexto, inclusive as que o declaravam**. O serviço recebe o pedido já montado, então nenhum teste dele veria isso; sem o E2E com o app real, a fatia teria sido entregue com a geração quebrada para todo usuário. **A armadilha do MVP-006 reapareceu e foi reconhecida:** o primeiro contrafactual do gate era `if (false)`, que fez o `typecheck` abortar o `build` — trocado por "tratar ausência de pack como diagnóstico", que compila e reprova pelo motivo certo. **Limites:** a busca estrutural não é executada pelo app (quem produz os caminhos é quem chama — executar `rg` seria o comando arbitrário que o MVP-004 fechou); `skills: () => []` no boot, que não é lacuna e sim o critério 5 no caminho real; `resumoAnterior` e `regras` existem no manifesto sem produtor até a M8-F03/F06; e a verificação visual do painel é do PI, porque sem consentimento do Google o AppShell não monta e a navegação pela sidebar fica fora do alcance do E2E.
>
> **`[FIX]` #172 entregue (2026-08-30).** O PI pediu, com screenshots: corrigir as combos, reduzir a informação por tela, separar em abas. O diagnóstico de raiz: o Settings era **anterior ao design system** (SPEC-Fundacao-05) e nunca foi re-plataformado — o idioma era `<select>` cru, o tema era botão cru, e oito seções de três MVPs se empilhavam num scroll único espremido em `max-w-xl`. **Cinco abas** (decisão do PI entre três opções): Geral · Permissões · IA · Roteamento · Conectores, agrupadas pelo **escopo do dado** — usuário à esquerda, usuário+espaço à direita. **Só a aba ativa monta**, afirmado por teste: as seções escopadas buscam dados ao montar, e a tela antiga disparava toda busca de todas as seções na abertura. **O roteamento era a queixa mais visível:** "SUBIR ANTHROPIC (CLAUDE API) EM CONVERSA" em três linhas era o nome acessível servindo de rótulo visível; virou `IconButton` de seta com a frase completa preservada para o leitor de tela — o olho tem a linha como contexto e só precisa da direção. **`Tabs` entrou no DS** (Radix, 15º primitivo da mesma família), não como CSS local: a próxima tela densa usa o componente. **Limite:** a verificação visual no app rodando é do PI, pela mesma razão do FIX #170.
>
> **`[FIX]` #170 entregue (2026-08-30).** O PI usou a tela da M8-F01 e achou um beco sem saída: o erro de `git` mandava ir a Terminal Controlado, e a tela não oferecia o caminho — a "próxima ação" que o `PRODUCT.md` exige existia como texto, não como affordance. A correção passa a morar **dentro do alerta que a pede**, pelo mesmo canal auditado da tela de Terminal (`permissions.change`, alto risco, decidido no main), com retomada da criação. **E o E2E escrito para verificar essa correção derrubou a premissa dela:** quando o `git init` falhava, a estrutura documental **já estava escrita no disco**, e a tentativa seguinte batia em `colisao` — instalar ou permitir o `git` e tentar de novo, que é o remédio que a própria mensagem manda aplicar, devolvia outra recusa. O botão nunca teria funcionado, e o beco sem saída teria só mudado de lugar. Falha de Git agora desfaz a estrutura que aquela chamada criou. O comentário que estava no código — *"o usuário fica com os arquivos e o remédio"* — era a racionalização do defeito. **Layout:** alinhamento pela âncora do input (o desalinhamento visível da screenshot), fim do cartão dentro de cartão, confirmação de remoção em faixa própria, side-stripe removido. **Corrigido na raiz, não só nesta rota:** o placeholder do AppShell era incondicional e anunciava "os módulos entram em fatias futuras" acima de **três** telas já entregues. **Uma guarda passava por acidente:** o teste "não expõe controle de Git" varria rótulos procurando a palavra e só rodava no estado sem erro — com um botão legítimo contendo "git", teria virado ruído; reescrito para afirmar que a tela nunca chama `runCommand`, com contrafactual. **Limite:** sem consentimento do Google o AppShell não monta, então a navegação pela sidebar fica fora do alcance do E2E — a verificação visual final é do PI.
>
> **M8-F01 entregue (2026-08-29) — abre o MVP-008.** Projeto local nasce, versiona e retoma: estrutura documental que espelha este repositório, `Project` + `PlanningSession` no SQLite, e **Git automático pelo terminal controlado**. **1358 testes** (+39: 21 de integração com Git real, 9 de regras, 9 de tela). **A decisão 2 do PI virou assinatura, não convenção:** o `GitRunner` recebe o `TerminalEngine` e nada mais cabe no construtor — não existe parâmetro por onde injetar um cliente de Git alternativo, e é assim que "nenhuma escrita de repositório por caminho paralelo" deixa de depender de alguém lembrar. **Colisão é detectada por consulta, nunca por exceção de `mkdir`:** descobrir a colisão pelo erro do FS já significaria ter começado a escrever, e o critério 3 proíbe diretório parcial — o teste marca o alvo com conteúdo do usuário e confirma que ele não mudou. **Criar projeto nunca amplia a allowlist** (critério 7): diretório fora dela é recusado com ação concreta, e o teste verifica as duas metades — nada escrito **e** lista idêntica. **Commit só em marco documental**, com mensagem determinística; o autosave do wizard grava no SQLite e não commita, porque cada tecla não é uma revisão. **Falha de commit não estoura** (critério 5): devolve `commitado: false`, o marco não é registrado e o rascunho fica intacto, então retomar tenta o mesmo commit. **O CRUD completo veio por pedido do PI, com duas decisões dele:** excluir **apenas desregistra** — pasta, arquivos e histórico Git permanecem no disco, e o `AuditEvent` declara `arquivosPreservados: true` em vez de deixar isso ser deduzido; e renomear muda **só o nome de exibição**, porque mover o repositório invalidaria todo caminho que já aponta para ele. **O contrafactual do critério 8 passou verde por defeito do próprio teste:** `montar(false)` construía um serviço com o `git` supostamente proibido, mas a allowlist vive no banco e o `beforeEach` já o havia permitido — o teste passava sem nunca exercitar a recusa. **A guarda anti-token do preload acusou `getPlanningSession`, e o falso positivo foi resolvido enumerando**, não afrouxando o regex: afrouxá-lo deixaria passar o `getAuthSession` de amanhã. **Limites:** só o marco `estrutura-inicial` é disparado nesta fatia (os outros cinco vêm com as fatias que os produzem); `PlanningSession.respostas` é mapa aberto até a M8-F03; e não houve smoke ao vivo com o app rodando — a fatia não chama serviço externo, o Git é local e a suíte o exercita de verdade.
>
> **M6-F05 e M6-F06 entregues (2026-08-29) — o MVP-006 fecha.** O conector de pesquisa: Search descobre, Extract confirma, e a evidência carrega o que a torna verificável. **O primeiro adapter que cobra** — implementa `custoEstimado`, e é ele que dá ao gate de créditos da F02 algo para decidir; até aqui o GitHub não cobrava e o gate nunca barrava. **1319 testes** (+129), **9 E2E** e **smoke real 13/13** contra a API (3 créditos, autorizados pelo PI). **Os critérios 1 e 6 da F06 falam de uma pipeline de `LANDSCAPE.md` que não existe no app** — quem a constrói é a M8-F04, cuja spec declara depender desta fatia; **decisão do PI**: a F06 entrega a *capacidade verificável* que ela vai chamar. **Dois hashes por evidência**, porque respondem perguntas diferentes: o do conteúdo detecta que a fonte mudou, o do trecho prova que a citação corresponde ao extraído. E a **deduplicação exige URL canônica *e* hash** — mesma URL com conteúdo divergente não é duplicata, é a fonte tendo mudado. **O teste de integração achou um defeito que o unitário não pegaria:** o 429 estava mapeado para `limite-excedido`, que a governança trata como terminal — o `retryable: true` era letra morta e o `Retry-After` nunca seria obedecido. **E o smoke real achou o que nenhum dublê acharia: a cobrança da extração não é local à chamada** — a Tavily acumula URLs entre chamadas e cobra 1 crédito a cada 5 no total (`0,0,0,0,1,0` em seis chamadas de 1 URL). O adapter passou a usar só o `usage` da resposta; quem protege a cota é o gate, por estimativa, antes de a chamada sair. **A armadilha da M5-F03 reapareceu e foi reconhecida:** um contrafactual de E2E deixou 9 testes verdes porque o `typecheck` abortou o `build` e o Playwright mediu o bundle anterior. **Limites:** o diretório `userData/artifacts/<run>/` não foi criado (escrever ali exige um `ExecutionRun`, que só a M8-F04 gera) e o teto de créditos segue sem tela.
>
> **M6-F04 entregue (2026-08-29).** As nove capacidades idempotentes do GitHub: repositório, issue, sub-issue, ref, PR, checks, actions, squash merge e merge state. **Todo `ensure*` procura antes de criar** — a única forma de "repetir não duplica" valer contra uma API sem upsert —, e a idempotência de issue vem de uma **chave externa no corpo**, não do título: título muda quando alguém reescreve, e a issue nasceria de novo a cada mudança. **1189 testes** (+74) e **8 E2E** no app real. **Dois pontos que a API cobra:** o `sha` no merge é o que faz o GitHub barrar head divergente com 409 (a garantia vem do serviço, não de uma comparação nossa que poderia estar velha), e o `merge_commit_sha` de PR **aberto** é um *test merge commit* que não está em branch nenhum — descartado, nunca repassado como merge real. **O E2E achou um defeito da F03 em produção:** o `validar` do adapter checava credencial, mas o `ConnectorService` o chama **antes** de resolver o cofre — então a guarda recusaria toda chamada ao GitHub; só tinha teste unitário, onde o secret é passado à mão. **E um contrafactual mostrou que o dublê mentia:** sem o `sha` no merge, o teste do head divergente passava, porque o fake recusava `undefined` enquanto a API real mergeia — corrigido o fake, o contrafactual reprova o teste certo. **Verificado no app real** com servidor que conta requisições: repetir `ensure` faz um POST só, e `verifyAuditChain` → `ok`. **O smoke real (17/17, repositório descartável autorizado pelo PI) achou o que nenhum fake acharia:** a listagem de issues do GitHub é eventualmente consistente — leva ~4 s para mostrar o que acabou de ser criado, e dois `ensureIssue` seguidos duplicariam a issue. A correção é reler depois de criar e convergir para a mais antiga. **Limites:** `repo.ensure` cria só na conta do usuário; a busca de issue pagina até 500; a fatia não tem UI, porque quem decide *quando* usar as capacidades é o MVP-009.

> **M6-F03 entregue (2026-08-29).** O desktop autentica no GitHub por **Device Flow**, sem client secret e sem private key — nem para o token inicial, nem para o refresh (a doc do GitHub dispensa o secret quando o token nasceu do device flow), e é isso que sustenta o critério 1 num app distribuído. **A emenda ao Vault entrou onde o PI a cravou**, sem reabrir a M5-F01: payload cifrado **estruturado** (access + refresh + expirações) numa escrita só, `expires_at` em coluna própria — legível **sem decifrar**, porque a tela pergunta "vence quando?" a cada render e responder pelo DPAPI faria toda consulta de estado destravar o segredo para ler um relógio — e **rotação atômica** por `INSERT … ON CONFLICT DO UPDATE`. **1115 testes** (+90) e **7 E2E** no Electron real. **Duas perguntas ao PI antes de codificar, as duas mudaram o construído:** a GitHub App do projeto não existe, então o `client_id` embutido fica **vazio** e o override em Configurações é o caminho que funciona hoje; e esse override mora no perfil, **fora do vault** — o `client_id` é público por desenho no Device Flow, e cifrá-lo o anunciaria como segredo que não é. **`ConnectorSecretSource.resolve` passou a aceitar `Promise`** — "renovar antes do uso" é chamada de rede e precisa acontecer no instante do uso; renovar por timer gastaria refresh em token que ninguém vai usar. **O contrafactual achou uma lacuna de cobertura, não um acerto:** apagar a credencial no ramo de "refresh recusado pelo GitHub" **passou** de primeira, porque os testes só exercitavam o refresh que falha por **rede**, nunca o que falha porque o serviço **respondeu** recusando; o teste que faltava foi escrito e só então o contrafactual reprovou. **Verificado no app real** com servidor HTTP local que **conta requisições**: o fluxo completa em exatamente **dois pollings**, nenhuma requisição carrega `client_secret`, nada do que volta pela ponte contém token, e `verifyAuditChain` → `ok` — e com um `client_secret` embarcado de propósito, o E2E reprova. **Limites:** sem smoke contra a API real (a App não existe); `auth.identify` é a única capacidade declarada, com repositório e issue ficando para a F04.
>
> **M6-F02 entregue (2026-08-29).** A governança que o ponto único da F01 existia para hospedar: gate de créditos, timeout com `AbortSignal`, retry seguro, circuit breaker e sanitização. **Nada do caminho de chamada foi refatorado** — só instrumentado, que é o critério 1 da F01 valendo na prática. O **ledger é em créditos e separado** do orçamento em USD do MVP-005 (decisão do PI): converter dependeria do plano contratado e produziria número falso, além de esconder qual dos dois estourou. **1025 testes** (+57), provados contra um **servidor HTTP local que conta requisições** — a decisão do PI que separa "o mock não foi chamado" de "a requisição não saiu". **O teste pegou o que a leitura não pegaria:** o `Retry-After` do serviço não era obedecido — o adapter o guardava como texto em `evidencia` e o serviço nunca o lia, então o backoff caía na curva; virou campo próprio e número. **E dois contrafactuais reprovaram a suíte, não o código:** inverter a ordem das recusas do retry passava despercebido (faltava o caso em que as duas se aplicam), e a guarda de repetibilidade dentro do serviço é **inalcançável** com a validação da F01 no lugar — ficou registrada como tal em vez de fingir redundância verificável. **Limites:** sem verificação no app real (não há conector registrado), `health()` sem chamador até a F03, e o teto de créditos sem tela.
>
> **M6-F01 entregue (2026-08-29) — abre o MVP-006.** O contrato único de capacidades externas, sem proxy HTTP genérico: cada adapter declara o que sabe fazer, valida sua entrada e devolve resultado ou erro **normalizado**. O que a fatia entrega de estrutural é o **ponto único de conectores** — a sede da governança que a F02 instala (health, retry, rate limit, ledger de créditos), pela mesma lógica que fez o `AiCallService` ser a sede do gate de orçamento. **Runtime separado do ponto único de IA** por decisão do PI: os dois compartilham Vault, auditoria encadeada e ledger, e nada mais. **968 testes** (+39: 24 Regras, 15 Banco), quatro provados por contrafactual. **Duas perguntas foram ao PI antes de codificar**, e as duas mudaram o construído: a F01 entrega canais tipados + listagem de capacidades (sem tela), e as chaves de credencial de conector viram **conjunto próprio** — acrescentá-las ao enum de IA faria o Settings anunciar credenciais ausentes que ninguém consegue usar até a F03. O cofre, esse, é o **mesmo**: `credential_ref.key` sempre foi texto, e o que é separado é a taxonomia, não o armazenamento. **O contrafactual pegou uma asserção que não provava o que dizia:** os testes de "recusa antes de I/O" afirmavam que o cofre não fora consultado, mas os pedidos não traziam credencial — a lista ficaria vazia mesmo com o defeito. Com credencial no pedido, reprovam dois em vez de um. **Limite registrado:** nenhum adapter concreto existe (GitHub é F03/F04, Tavily é F05/F06), então não há verificação no app real — o que os testes exercitam é o *contract fixture* que a spec pede, e a prova em Electron chega com o primeiro adapter.
>
> **M5-F01 entregue (2026-08-28) — abre o MVP-005.** Os 9 critérios cobertos; **679 testes verdes** (+38: 9 Regras, 20 Banco, 9 Tela). A garantia central é **estrutural**: nenhum tipo que atravessa o IPC tem campo onde o segredo caiba, e não existe método na ponte que o peça. Verificado no app real — o segredo semeado aparece **0 vezes** em `jarvis.db`/`-wal`/`-shm` enquanto `credential_ref` aparece 3 (prova de que a busca funciona); é a prova do **DPAPI real**, já que o teste de integração usa cifra dublada. `verifyAuditChain` → `{ok: true, checked: 95}`. Detalhe em `DEVELOPMENT.md`.
>
> **A verificação achou um defeito do design system, não da fatia** ([#107](https://github.com/RodReis/rrb-jarvisOS/issues/107)): overlays em portal renderizam **sem tokens** — modal transparente e ilegível. O `ProvedorDeTema` injeta as variáveis num `div`, o Radix monta o portal no `body`, fora dela. Atinge os 5 componentes com portal, é anterior a esta fatia. **Terceira repetição do mesmo método no projeto** (depois de #57/#58): jsdom não aplica folha de estilo, então componente visualmente quebrado passa verde.
>
> **#107 entregue (2026-08-28).** Cada `Portal` recebe o nó do provider como `container` — preserva os providers aninhados que a CHOICE e o Settings usam, o que promover os tokens a `:root` quebraria. A verificação no app real achou um **segundo defeito com a mesma causa raiz**, escondido pelo primeiro: o painel herdava a cor de texto do `FundoDaIdentidade`, que o portal não tem, e o título caía no preto do navegador. **684 testes** (+5) e **82 provas de navegador** (+5), todos provados por contrafactual. A régua nova é em duas camadas por necessidade: jsdom só afirma topologia, o valor computado só o navegador mede — que é exatamente o buraco pelo qual este defeito passou.

> **M5-F02 entregue (2026-08-29).** A IA passa a chamar de verdade. O que a fatia entrega de estrutural é o **ponto único de chamada** — a sede do gate de orçamento da F03 (ADR-001 q1); um segundo caminho até um adapter seria um caminho sem gate. O **isolamento do provider virou regra do ESLint** (barra `@anthropic-ai/*` em todo `src/main/` menos o adapter), provada por contrafactual. **732 testes** (+48) e 2 E2E novos; verificado no app real com Electron, com o log do app mostrando `correlationId`, `AuditEvent` e custo medido. A cobertura de Regras caiu para 75.5% por **diluição**, não regressão: `ai.ts` está a 100% ali, e o que cresceu (`preload`, `handlers`) é coberto por Banco e E2E.
>
> **#84 entregue (2026-08-29).** O card da fila descrevia **toda** pendência como filesystem — uma execução de processo aparecia como "Filesystem: comando", sem binário, argumentos ou cwd, e o usuário aprovava às cegas. A correção **não inventa discriminante**: reusa o `operation.kind` que o handler de `approval:resolve` já usava para rotear a decisão entre os dois motores — o dado existia, só a apresentação o ignorava. **735 testes** (+3, todos de Tela), provados por contrafactual, e o ramo de filesystem intacto. Verificado no app real: o card lê "Executar comando: node --force" com o cwd no escopo, e **Aprovar continua executando** (`verifyAuditChain` → `{ok: true, checked: 125}`). O achado que vale registrar é de método: o componente não tinha **nenhum** teste — o defeito não passou por asserção frouxa, passou por ausência de suíte.
>
> **M4-F03 entregue (2026-08-29).** A tela que faltava desde o MVP-002: os canais da allowlist de diretórios existiam na ponte e **nenhuma tela os usava**, então pelo aplicativo ninguém conseguia permitir uma pasta — e sem isso o terminal recusava todo cwd. A fatia **não toca enforcement**; entrega o acesso a ele. **Adicionou dois canais IPC, não um** como a spec previa: o critério 4 pede marcar o `appDir` como fixo, mas `listAllowedDirectories` devolve strings sem marcação e o `appDir` nunca atravessava o IPC identificado — as alternativas eram mudar um contrato que a spec proíbe mudar, ou inferir a identidade por posição no renderer (acoplando a UI à ordem de inserção do `Set` no repositório). **Decisão do PI: canal só-leitura `allowlist:app-dir`.** **750 testes** (+15: 4 Regras, 11 Tela) e **+1 E2E**; quatro provados por contrafactual. **A revisão do próprio código achou um defeito que os 9 primeiros testes não pegavam:** o `ErrorState` era irmão do conteúdo em vez de substituí-lo, e falhando a carga o usuário via o aviso **junto de uma lista vazia com o botão de permitir** — "você não tem nenhuma pasta" quando o certo é "não sabemos quais são". O teste de erro passava porque afirmava só o **texto** do alerta, nunca o que estava ao lado dele. **Verificado no app real pela ponte real:** a pasta recusada por `cwd-fora-da-allowlist` passa a executar `node --version` → `v24.15.0` depois de permitida, o `appDir` resiste à remoção e `verifyAuditChain` → `{ok: true, checked: 10}`. **Verificada renderizada, com a sessão logada:** os valores computados confirmam o que jsdom não mede — tokens do tema resolvidos, `Tag` do `appDir` distinta dos botões de perigo, altura de controle 44px, **sem overflow horizontal** com paths longos, anel de foco de 3px por teclado. Remover **pela tela** tira o item; o `appDir` não tem botão; `verifyAuditChain` → `{ok: true, checked: 129}`. **E a tela expôs o que a ausência dela escondia:** duas entradas-lixo gravadas por uso manual da ponte nas verificações anteriores — resíduo do `resolve()` sobre path não-absoluto, que o seletor nativo torna impossível.
>
> **M5-F03 entregue (2026-08-29).** O `CostEvent` que a F02 media vira **enforcement**: antes de cada chamada, o ponto único compara (gasto real acumulado no período) + (estimativa desta chamada) contra os limites do escopo, e a chamada que estouraria **não sai**. O gate encaixa exatamente onde a F02 o preparou, e **antes** da auditoria de requisição — barrar depois de o `AuditEvent` dizer "requisitei" registraria uma requisição que nunca houve. **`cost_event` precisou ser tabela nova:** a F02 emitia o custo só como payload de `audit_event`, e payload de cadeia append-only não se consulta por período. **Piso de bloqueio duro por decisão do PI** — o MVP-004 está entregue, mas sua fila de aprovação é acoplada a `run_id`/`step_id` de workflow, que chamada de IA não tem; o override por aprovação segue como alvo de fatia futura. **824 testes** (+73) e **3 E2E**. **O teste pegou o que a leitura teria deixado passar:** o caminho barrado passava por `finalizar` e **registrava `CostEvent` de uma chamada que nunca saiu** — e o comentário que eu havia escrito ali afirmava o contrário do que o código fazia; quem pegou foi a asserção que **conta linhas**, não a que lê a mensagem. **E o contrafactual quase mentiu duas vezes**, que é a lição de método da fatia: (1) `npm run build` roda `typecheck` antes, então um contrafactual que não compila aborta o build e o Playwright roda contra o **bundle anterior** — "3 passed" sem ter provado nada; (2) auditar `decisao: 'bloqueado'` **não prova enforcement**, porque o serviço decide igual em report-only e quem barra é o ponto único — o que separa os dois é a **ausência da fase `requisicao`**. **Verificado no app real** com servidor SSE local que **conta requisições**: a chamada barrada registra **zero** no provider e US$ 0,00 de gasto; o estouro no meio do stream deixa a primeira concluir e barra a **segunda** com o contador ainda em 1; `verifyAuditChain` → `ok`. **Limite registrado:** a prova de **valor computado** da tela (cor da barra no limiar, 44px, anel de foco) ficou pendente — sem a stack Supabase no ar o app fica no login e Settings não é alcançável pela UI. Decisão do PI: entregar assim e registrar.
>
> **M5-F04 entregue (2026-08-29) — o MVP-005 fecha.** Três adapters novos (Gemini HTTP, Ollama HTTP local, Claude Code CLI subprocess) pela **mesma interface** da F02, e o ponto de chamada **não mudou** por causa deles — o critério 1 valendo na prática. Todos herdam classificação, `CostEvent`, `AuditEvent` e o **gate de orçamento da F03**: nada burla o ponto único. O `ProviderRoute` decide quem atende cada tipo de tarefa, com preferência local e **fallback auditado**. **929 testes** (+71) e **6 E2E**. **A mudança mais arriscada foi tornar `apiKey` opcional** — Ollama e `claude-code` não têm credencial, mas com `undefined` aceito, marcar um provider **pago** como sem-credencial passaria em silêncio; duas guardas novas fecham (só rota `unmetered` dispensa credencial, e toda rota unmetered custa **exatamente** zero). **O `AiCallHandle` deixou de afirmar o que não sabia:** o handler devolve o handle antes da seleção, então prever o provider erraria em todo fallback — o realmente usado chega no `CostEvent`. **O teste pegou o que a leitura não pegaria:** o mock da ponte no `AppShell.test.tsx` estava incompleto e **a suíte passava assim mesmo**, com a tela em estado de erro — verde, mas não é o Settings que o usuário vê. **Verificado no app real no pior cenário** (sem credencial e sem Ollama): a rota sem ninguém disponível **recusa a chamada** em vez de gastar às cegas, e `verifyAuditChain` → `ok`. **A armadilha da F03 reapareceu e foi reconhecida na hora** — `npm run build` roda `typecheck` antes, e um build que aborta deixa o Playwright medindo o bundle anterior.
>
> **Decisões do PI (2026-08-29):** três modelos na tabela de preço (Opus 5, Sonnet 5, Haiku 4.5) para dar spread à F03 e à F04; **SDK oficial** `@anthropic-ai/sdk` em vez de `fetch` cru; **painel mínimo** de teste no Settings — a tela de providers é F04, explicitamente.

## MVPs

| MVP | Issue | Estado | Progresso |
|---|---|---|---|
| MVP-001 Fundação | [#1](https://github.com/RodReis/rrb-jarvisOS/issues/1) | fechado/aceito | 6/6 |
| MVP-002 Execução local | [#9](https://github.com/RodReis/rrb-jarvisOS/issues/9) | fechado/aceito | 5/5 |
| MVP-003 Design System | [#16](https://github.com/RodReis/rrb-jarvisOS/issues/16) | fechado/aceito | 8/8 |
| MVP-004 Execução real | [#10](https://github.com/RodReis/rrb-jarvisOS/issues/10) | **três fatias finalizadas**; o épico aguarda o fechamento do PI | 3/3 |
| MVP-005 Providers/Vault/Budget | [#76](https://github.com/RodReis/rrb-jarvisOS/issues/76) | F01 e F02 finalizadas; **F03 e F04 entregues**, aguardando aceite | 4/4 |
| MVP-006 Conectores Essenciais | [#86](https://github.com/RodReis/rrb-jarvisOS/issues/86) | **as seis fatias entregues**, aguardando aceite; o épico aguarda o fechamento do PI | 6/6 |
| MVP-007 Memória compartilhada | [#179](https://github.com/RodReis/rrb-jarvisOS/issues/179) | épico e 8 fatias pré-criadas em `proplan:planejado` (2026-08-30); SPEC da F01 em `rascunho`, **só na branch `codex/pipeline-v2-design`** | 0/8 |
| MVP-008 Planejamento Governado | [#93](https://github.com/RodReis/rrb-jarvisOS/issues/93) | F01–F05 finalizadas; F06 entregue, aguardando aceite; o épico aguarda o fechamento do PI | 6/6 |
| MVP-009 Entrega Autônoma | [#100](https://github.com/RodReis/rrb-jarvisOS/issues/100) | seis SPECs `aprovada-pi` (2026-08-29), **emendadas pelo PI em 2026-08-30**; F01 em andamento | 0/6 |
| MVP-010 a MVP-016 (Pipeline V2/V3) | [#115](https://github.com/RodReis/rrb-jarvisOS/issues/115) · [#121](https://github.com/RodReis/rrb-jarvisOS/issues/121) · [#127](https://github.com/RodReis/rrb-jarvisOS/issues/127) · [#133](https://github.com/RodReis/rrb-jarvisOS/issues/133) · [#149](https://github.com/RodReis/rrb-jarvisOS/issues/149) · [#155](https://github.com/RodReis/rrb-jarvisOS/issues/155) · [#162](https://github.com/RodReis/rrb-jarvisOS/issues/162) | 7 épicos e 35 fatias pré-criadas (#116–#168); SPECs e docs de MVP **só na branch `codex/pipeline-v2-design`** (enviada ao remoto em 2026-08-30; PR de docs pendente). Fora da fila até chegar à `main` | 0/35 |
| MVP-017 a MVP-021 (Command Center) | [#193](https://github.com/RodReis/rrb-jarvisOS/issues/193) · [#194](https://github.com/RodReis/rrb-jarvisOS/issues/194) · [#195](https://github.com/RodReis/rrb-jarvisOS/issues/195) · [#196](https://github.com/RodReis/rrb-jarvisOS/issues/196) · [#197](https://github.com/RodReis/rrb-jarvisOS/issues/197) | 5 épicos criados em 2026-08-30, aprovados pelo PI na mesma data (HA removido; voz local Whisper+Piper): voz+persona+mascote, escuta contínua, briefing/proatividade, integrações Gmail/Agenda/Spotify, visão Frigate. Fatias lazy — nenhuma SPEC ainda; docs em `docs/mvp/mvp-017…021` | 0/18 |

> **MVP-006 aprovado (2026-08-29).** As seis SPECs passaram pelo gate de perguntas abertas e viraram `aprovada-pi`. Sete decisões do PI: runtime de conectores **separado** do ponto único de IA do MVP-005; **ledger de créditos próprio** para conector, independente da `BudgetPolicy` em USD; **GitHub App do projeto** com `client_id` embutido e override em Settings; **emenda do Vault para OAuth** (payload estruturado, `expires_at`, rotação atômica) como escopo da M6-F03, sem reabrir a M5-F01; **roteamento Context7↔Tavily removido** da M6-F05 e transferido ao MVP-008; **UI mínima dentro de cada fatia**, sem fatia dedicada; **evidência extensa no diretório de artefatos do app**. A aprovação **não muda a fila** — a cabeça continua sendo a M5-F02 (#78).

> **MVP-008 aprovado (2026-08-29).** Seis decisões do PI: projeto nasce **sob `userData`** e criar projeto nunca amplia a allowlist; **Git é o `git` do sistema pelo terminal controlado do MVP-004**, sem segundo caminho de escrita fora do enforcement; **importar o próprio `rrb-jarvisOS`** é critério de aceite; a **rota de assinatura (Claude MAX via Claude Code) registra uso sem valor monetário** e a `BudgetPolicy` gateia só rota paga; o app **não chama Context7** — ele é do agente construtor no MVP-009; **anexos de design entram por seletor que copia e hasheia no ato**. Duas dependências duras ficaram registradas: a M8-F01 depende da M4-F02 (#75) e fica limitada ao diretório do app até existir a fatia de UI da allowlist. Emendas em `spec-providers-03` e `spec-providers-04`: rota de assinatura é `unmetered`. **A fila não mudou** — a cabeça continua sendo a M5-F02 (#78).

> **Revisão de furos do MVP-009 (2026-08-30).** Auditoria das seis SPECs contra o board e o código entregue achou quatro furos que impediriam a pipeline de funcionar e quatro decisões do PI os fecharam: **(1)** "nenhum segredo entra no container" deixava o Claude Code sem como chamar modelo — a assinatura vive em `~/.claude` do host; resolvido por **proxy no host** (critério 9 continua literal). **(2)** `checksAprovam` da M6-F04 recusa lista vazia e o projeto gerado não tem CI — nenhum run chegaria a `MERGED`; resolvido: **a pipeline gera o workflow** no primeiro PR, e sem check configurado nunca é verde. **(3)** WIP=1 passa a **slot global**; concorrência é o MVP-012. **(4)** A branch `codex/pipeline-v2-design` (33 commits só locais, com as SPECs dos MVPs 7 e 10–16 **e emendas às SPECs aprovadas do MVP-009 e à CONVENTION**) foi enviada ao remoto e entra por PR de docs; as emendas que contradizem decisão registrada — redefinir `finalizado` como "encerramento administrativo" — não valem até o PI decidir no PR. Furos menores fechados por emenda (Cowork, PI pode vetar): estado terminal `AWAITING_MERGE` que F05/F06 exigiam e F02 não tinha; `AWAITING_PI` = gate `SLICE_ENTRY`, sem segundo "go"; fonte dos paths permitidos (a SPEC gerada não os traz); validações do app rodam no container; revisor = o próprio executor; `STATUS.md`/relatório do projeto-alvo entram no PR, não em commit pós-merge; relatório em `reports/`, não `docs/`; na F01, Device Flow em vez de "instalação", proteção de branch recusada vira limitação, rótulos vêm da Convention do alvo, `ExternalRef` é saída. **Alerta registrado:** 34 SPECs viraram `aprovada-pi` em 29/08 com "Perguntas abertas: nenhuma" — o gate de perguntas não rodou nelas.

> **MVP-009 aprovado (2026-08-29).** Três decisões do PI: **o container Docker é o sandbox do executor** — o Claude Code roda nele com o worktree montado, nunca no host; **merge autônomo ligado por padrão com kill-switch por projeto**, e desligado o run termina no PR verde aguardando o PI; **Context7 é ferramenta do agente construtor** na M9-F04, fechando a pendência herdada do MVP-008. A decisão do container fecha um buraco real: o MVP-004 proibiu comando arbitrário, mas um agente que constrói software precisa exatamente disso — sem fronteira nova, o MVP-009 passaria por cima do enforcement que o MVP-004 entregou. **Docker passa a ser dependência dura**, sem fallback para o host.

## Índice Fatia ↔ SPEC

Não existe catálogo global `SPEC-nnn`. O identificador canônico é o slug abaixo; não inventar números.

| Índice | MVP | Fatia | SPEC |
|---|---|---|---|
| F01–F06 | MVP-001 | Bootstrap · AppShell · Auth · Dados · Settings · Observabilidade | `spec-fundacao-01..06` |
| M2-F01–F05 | MVP-002 | Supabase · Policy · Allowlist · Workflows · Simulação | `spec-execucao-local-01..05` |
| M3-F01–F06 | MVP-003 | Infra · Foundations · Componentes a/b · AppShell a/b · Identidades · Hardening | `spec-design-system-01..06` |
| M4-F01 | MVP-004 | Filesystem allowlisted | `spec-execucao-real-01-filesystem-allowlisted.md` |
| M4-F02 | MVP-004 | Terminal controlado | `spec-execucao-real-02-terminal-controlado.md` |
| M4-F03 | MVP-004 | UI da allowlist de diretórios ([#110](https://github.com/RodReis/rrb-jarvisOS/issues/110)) | `spec-execucao-real-03-ui-allowlist-diretorios.md` |
| M5-F01 | MVP-005 | Vault | `spec-providers-01-vault-credenciais.md` |
| M5-F02 | MVP-005 | Adapter Claude | `spec-providers-02-adapter-claude-api.md` |
| M5-F03 | MVP-005 | BudgetPolicy | `spec-providers-03-budget-policy.md` |
| M5-F04 | MVP-005 | Multi-provider | `spec-providers-04-multi-provider-roteamento.md` |
| M6-F01 | MVP-006 | Núcleo de conectores | `spec-conectores-01-nucleo.md` |
| M6-F02 | MVP-006 | Operação e governança | `spec-conectores-02-operacao-governanca.md` |
| M6-F03 | MVP-006 | GitHub App/autenticação | `spec-conectores-03-github-app-autenticacao.md` |
| M6-F04 | MVP-006 | Automação GitHub | `spec-conectores-04-github-automacao.md` |
| M6-F05 | MVP-006 | Tavily Search | `spec-conectores-05-tavily-search.md` |
| M6-F06 | MVP-006 | Tavily Extract/evidências | `spec-conectores-06-tavily-extract-evidencias.md` |
| M8-F01 | MVP-008 | Projeto e Git local | `spec-planejamento-01-projeto-git-local.md` |
| M8-F02 | MVP-008 | Contexto, skills e orçamento | `spec-planejamento-02-contexto-skills-orcamento.md` |
| M8-F03 | MVP-008 | Wizard orientado | `spec-planejamento-03-wizard-orientado.md` |
| M8-F04 | MVP-008 | PRD/Landscape/Convention | `spec-planejamento-04-prd-landscape-convention.md` |
| M8-F05 | MVP-008 | Anexos/design/arquitetura | `spec-planejamento-05-anexos-design-arquitetura.md` |
| M8-F06 | MVP-008 | Roadmap e aprovações | `spec-planejamento-06-roadmap-aprovacoes.md` |
| M9-F01 | MVP-009 | Publicação GitHub | `spec-entrega-01-publicacao-github.md` |
| M9-F02 | MVP-009 | DAG/fila/reconciliação | `spec-entrega-02-dag-fila-reconciliacao.md` |
| M9-F03 | MVP-009 | Worktree/preflight/Docker | `spec-entrega-03-worktree-preflight-docker.md` |
| M9-F04 | MVP-009 | Construção/recuperação | `spec-entrega-04-construcao-recuperacao.md` |
| M9-F05 | MVP-009 | Revisão/CI/merge | `spec-entrega-05-revisao-ci-merge.md` |
| M9-F06 | MVP-009 | Evidência/limpeza | `spec-entrega-06-evidencia-limpeza-continuidade.md` |

## Próximas ações

1. **O MVP-005 fechou**: as quatro fatias estão finalizadas (#77–#80); só falta o PI fechar o épico [#76](https://github.com/RodReis/rrb-jarvisOS/issues/76). O mesmo vale para o MVP-006 ([#86](https://github.com/RodReis/rrb-jarvisOS/issues/86)) — seis fatias finalizadas, épico aberto.
2. **O MVP-004 ([#10](https://github.com/RodReis/rrb-jarvisOS/issues/10)) tem as três fatias finalizadas** e só falta o PI fechar o épico.
3. **A M6-F03 herda dois pontos prontos**: o ponto único com governança (F01+F02) e a **emenda ao Vault** que o PI cravou como escopo dela — payload cifrado estruturado (access + refresh + `expires_at`) e rotação atômica. O `ConnectorAdapter` já tem `health()` opcional esperando o primeiro serviço a sondar.
4. **MVP-006, MVP-008 e MVP-009 revisados e aprovados (2026-08-29)** — dezesseis decisões estruturais do PI registradas nas SPECs e nos docs dos épicos; #87–#92, #94–#99 e #101–#106 migraram para `proplan:backlog`. **Todas as 24 SPECs do projeto estão `aprovada-pi`.**
5. MVP-007 será detalhado apenas quando entrar no planejamento ativo — hoje não tem fatia nem SPEC, então não há o que aprovar.
6. **A M4-F03 fechou a última fatia conhecida sem SPEC** e foi entregue no mesmo dia: não existe mais fatia conhecida sem spec, e o uso real do terminal/filesystem pelo app deixou de depender da ponte — a M8-F01 perde o limite de criar projeto só dentro do diretório do app.
7. **Docker virou dependência dura do MVP-009** (sandbox do executor). Confirmar que a máquina de execução tem Docker antes daquele MVP entrar na fila.
8. **PR de docs da branch `codex/pipeline-v2-design`** (aberto em 2026-08-30 como rascunho): o PI decide, item a item, as emendas que mudam decisão registrada (`finalizado` como encerramento administrativo; `EffectJournal` numa SPEC já finalizada; segredo mínimo no container — já superado pela decisão do proxy). Só depois do merge as SPECs dos MVPs 10–16 contam como `aprovada-pi` para a fila.
9. **#170 está `closed` com `proplan:done`** — trocar para `proplan:finalizado` (carimbo do PI) para o board projetar a coluna certa.
10. **Corpo da #101** ainda dizia "não iniciar até `aprovada-pi`" — corrigido em 2026-08-30; o Code deve ler § Emendas da SPEC-Entrega-01 antes de fechar a entrega.

## Roadmap

MVP-001 ✅ → MVP-002 ✅ → MVP-003 ✅ → MVP-004 → MVP-005 → MVP-006 → MVP-008 → MVP-009. MVP-007 é paralelo/não bloqueante. Depois do MVP-009 entra a série do Command Center: MVP-017 → MVP-018/MVP-019 (paralelos entre si) → MVP-020 → MVP-021 (decisão do PI, 2026-08-30); os MVP-010–016 permanecem fora da fila até o PR de docs da `codex/pipeline-v2-design` chegar à `main`.
